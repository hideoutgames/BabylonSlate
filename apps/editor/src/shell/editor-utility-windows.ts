import type { DockWindowDefinition } from "./window-catalog";

/** P12 seam: EditorUtilityInterface widgets opened from Windows → Editor Utilities. */
export function listEditorUtilityWindows(): DockWindowDefinition[] {
  return [];
}
