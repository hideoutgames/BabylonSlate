# Scene editing (P6)

Shared-surface design note for viewport, outliner, details, and the edit layer. Authoritative schema: `packages/core/src/scene.ts` (`SCENE_SCHEMA_VERSION = 3`). **One scene document tab at a time** — opening a scene closes the previous (Unsaved: Save / Discard / Cancel). Graphs and Content Browser stay. Idle-unmount (`p18-inactive-documents`) does **not** close the scene tab: JSON and undo stay; overlay Play still uses that open document if the viewport has unmounted.

## SerializedScene v3

| Field          | Role                                                                                                 |
| -------------- | ---------------------------------------------------------------------------------------------------- |
| `name`         | Scene display name                                                                                   |
| `viewportMode` | `"3d"` \| `"2d"` — per-scene default; toolbar toggle always available                                |
| `settings`     | Environment, gravity, timestep, fog, grid, `cameraBounds2D`, `editorJoystickEnabled`. `gameInstanceClass` stays in the schema for old files; Play/Preview/export read `ProjectSettings.gameInstanceClass` |
| `actors`       | Flat list with `parentId`; hierarchy resolved at apply time                                          |
| `folders`      | Editor-only Outliner folders (`id`, `name`, `parentFolderId`); missing documents normalize to `[]`   |

Each **actor**: `id`, `name`, `classId`, `parentId`, `transform` (position / quaternion rotation / scale), `visible`, `locked`, `components[]`, `folderId`. Details shows **Position**, **Rotation**, and **Scale** as one nowrap XYZ row each. Rotation is **Euler degrees** in the UI; `transform.rotation` stays `[x,y,z,w]` quaternion (`quaternionToEulerDegrees` / `eulerDegreesToQuaternion` in `@babylonslate/core`). 2D drops unused axes (Position/Scale XY, Rotation Z).

Each **component**: `id`, `classId`, `properties` (typed per class in the object-model registry), optional `parentId`, optional `transform` (local position / quaternion / scale relative to the parent component, or the **actor origin** when `parentId` is null). Missing `transform` normalizes to identity — no `SCENE_SCHEMA_VERSION` bump.

**Scene settings** include `physicsWorld` (`"3d"` \| `"2d"`, defaults from `viewportMode`), `grid` (`snapEnabled`, translate/rotate/scale snap, `tileSize`, `tileSubdivisions`, `showGrid`), `cameraBounds2D` (`width`, `height`) for the 2D game-camera frame overlay, and `editorJoystickEnabled` (on-screen stick that flies/pans the **editor** camera; default true, missing keys normalize to true, explicit `false` stays off). `grid.showGrid` is additive (missing keys normalize to true). A scene never mixes physics worlds — see [physics.md](physics.md). Details and Actor Prefab Add Component lists include `RigidBodyComponent` and `ColliderComponent` (defaults from `parseRigidBodyProperties` / `parseColliderProperties`). Prefab add uses the open scene’s `physicsWorld` and editor `viewportMode` when a scene is open (otherwise 3d).

**Actor ids are unique after normalize.** A repeated `id` collapsed the pair into one editor mesh and one Play snapshot slot, so the duplicate silently vanished. `normalizeScene` renames later occurrences (`dupe` → `dupe-2`); the first owner keeps the id, so existing `parentId` references still resolve.

## Outliner folders

Folders organize the Outliner. They are **not** actors and never reach the object model, so Play spawns the same actors with or without them.

| Concern | Field | Runtime |
| --- | --- | --- |
| Grouping | `SerializedScene.folders[]` + `actor.folderId` | Ignored |
| Transform attachment | `actor.parentId` | Authored hierarchy |

- Folders nest through `parentFolderId`. `normalizeScene` drops malformed rows, uniquifies ids, and resets dangling or cyclic parents to the root so a folder can never be unreachable.
- **Drop targets decide meaning**: on a folder → `folderId` (transform parent cleared); on an actor → `parentId` (folder cleared, inherited from the new parent); on empty space → both cleared. Transform children follow their parent actor regardless of folder. If the dragged row is selected, `outlinerTreeDropMoves` moves collapsed roots only (omit nested selected folders, transform children of selected parents, and actors inside a selected folder subtree). Folder roots dropped on an actor no-op the whole selection. Unselected-row drag still moves that one row. In-tree drops never duplicate actors.
- **Delete promotes**, never destroys: child folders and actors move up to the deleted folder's parent.
- Folder rows have no visibility or lock toggles. Exclusive folder tap clears the actor selection so it cannot drive the gizmo or Details. Additive / range can mix folder and actor rows; the viewport gizmo still uses `selectedActorIds` only.
- Search matches folder names and shows the folder path for actor hits (`Lighting / Spots / Lamp`).
- Create / rename / reparent / delete and actor moves are `scene.addFolder`, `scene.renameFolder`, `scene.reparentFolder`, `scene.removeFolder`, and `scene.setActorFolder` — reversible commands on the existing undo stack.

**Load always runs `normalizeScene`.** `ProjectService.loadDocument("scene")` coerces the file payload through `normalizeScene` so additive `SceneSettings` fields (`grid`, `environmentColor`, `cameraBounds2D`, …) exist in memory even when a current-version (v3) asset omitted them. Do not bump `SCENE_SCHEMA_VERSION` for additive keys. Files are not rewritten on load (migrate-on-save still requires approval). Play already normalized; the editor document tab now does too. Opening an old scene without this step threw in Details (`settings.grid` / `environmentColor`) and unmounted the editor.

## Selection model

`SceneEditingProvider` (`apps/editor/src/context/scene-editing-context.tsx`) is shared by viewport, outliner, and details:

- `selectedActorIds`, `selectActor` (single or additive), `setSelectedActorIds`, `isSelected`. Outliner Ctrl/Shift/Meta click and viewport Ctrl/Meta/Shift tap call `selectActor(id, additive)` for actors; Outliner additive also toggles folders in panel state. TreeView two-finger tap range-selects visible Outliner rows (folders and actors). Details with more than one actor uses a **N Actors** heading.
- `gizmoTool` / `setGizmoTool` (`translate` \| `rotate` \| `scale` \| `none`)
- `dragSelectActive` / `setDragSelectActive` — one-shot Drag Select; unpresses after the next tap or marquee
- `snapEnabled` / `setSnapEnabled` — live viewport settings; persisted via `settings.grid.snapEnabled`
- `gridVisible` / `setGridVisible` — live viewport settings; persisted via `settings.grid.showGrid` (default true)
- `joystickEnabled` / `setJoystickEnabled` — live viewport settings; persisted via `settings.editorJoystickEnabled` (default true)
- Overlay Play **freecam** mounts the same fly stick (`PlayFreeCamJoystick`) while the debug camera is on; it is not the scene `editorJoystickEnabled` flag.
- `viewportMode` / `setViewportMode` — live mode synced from `documentViewportMode` so undo/redo of `SetViewportModeCommand` restores the camera, not only the serialized field

Panels never mutate selection independently — they consume `useSceneEditing()`.

## Command layer

Every scene Details / outliner / viewport mutation routes through `applySceneChange` → `diffSceneCommands` → the undo stack. Notable command types:

| Command                    | Diff trigger                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------- |
| `SetSceneNameCommand`      | `scene.name`                                                                                |
| `SetViewportModeCommand`   | `scene.viewportMode`                                                                        |
| `SetSceneSettingCommand`   | any `settings.*` field (including `grid.snapEnabled`, `grid.showGrid`, `gameInstanceClass`) |
| `ReorderComponentCommand`  | component list order change with the same ids                                               |
| `SetActorTransformCommand` | single-actor gizmo drag / Details transform (merge key `transform:{actorId}`)               |
| `SetActorsTransformsCommand` | multi-select gizmo drag (merge key `transforms:{sortedIds}`) — one undo for the group     |

`RemoveActorCommand` stores a single-actor snapshot (not a full subtree). UI deletes that remove a hierarchy emit one remove per actor.

## Gizmo + camera contracts

Both systems are **mode-parametric** via `ViewportMode` from `@babylonslate/core`:

| Mode | Camera                                                                                                                                                                 | Gizmo                                                |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| `3d` | Fly/look: WASD + one-finger (or LMB) look-in-place, pinch/wheel zoom, three-finger pan. **Pivot Around Center** (Viewport Settings, session-only) makes one-finger drag and the editor joystick orbit the current look-at point instead | Full translate / rotate / scale                      |
| `2d` | Orthographic; WASD/joystick XY pan, one-finger pan 1:1 with the pointer (hold-then-move marquee), pinch zoom, three-finger pan at the same 1:1 scale (look is a no-op) | XY translate + XY plane; Z rotate; XY scale; unused axes and YZ/XZ planes hidden |

**2D convention** (fixed, left-handed Babylon): content on the **XY plane**, **+Y up**, **+X right**, editor camera at **−Z** looking toward **+Z**. `scene.useRightHandedSystem` stays `false`.

**Per-mode pose memory** (in-session, not serialized): `setMode` snapshots the live camera before switching and restores the destination mode's last pose. 3D stores target / alpha / beta / radius; 2D stores target / ortho half-height / pixel zoom. The first visit to a mode keeps the live target (so 3D→2D still looks at the same place) and applies that mode's defaults. Poses live on the editor camera controller (`exportSessionState` / `importSessionState`) and on the document `SceneEditingProvider` (a ref, so Focus exit `fromJSON` and Windows close/reopen of Viewport or Prefab can remount the canvas without resetting the view). **Pivot Around Center** is the same session lifetime (off by default; not scene JSON, not undo). Closing the document tab still uses the default camera.

Implementation: `editor-camera.ts`, `gizmo-host.ts`, `gizmo-multi-select.ts` in `@babylonslate/render`. Gizmos stay Babylon `PositionGizmo` / `RotationGizmo` / `ScaleGizmo` on a utility layer. Restyle (not a custom mesh engine): unlit `disableLighting` + emissive axis `Color3`s (`GIZMO_AXIS_COLORS`, paired with chrome `--axis-x/y/z` in [theming.md](theming.md)), `GIZMO_SHAFT_THICKNESS` 0.45, `GIZMO_ROTATION_THICKNESS` 8, rotation tessellation 64, planar translate squares at idle alpha ~0.16 / hover ~0.42, hover brightens the active axis. `scaleRatio` defaults to `DEFAULT_GIZMO_HANDLE_SCALE` 1.8 (visuals stay compact). Invisible translate/scale leaf colliders scale by `GIZMO_COLLIDER_SCALE` 2.5; rotation keeps Babylon’s same-diameter fatter torus collider (do not uniformly scale the pick ring). Visible translate cones and scale boxes scale by `GIZMO_END_CAP_SCALE` 1.6 (shafts stay thin). `ScaleGizmo.sensitivity` is `GIZMO_SCALE_SENSITIVITY` 10 so drag strength is not weakened by handle size. Uniform scale keeps Babylon’s small unlit octahedron (`GIZMO_UNIFORM_COLOR` on `ScaleGizmo.coloredMaterial`) — do not `setCustomMesh` a world-sized cube. Keep `gizmoAxisEnabledFlags` (2D hides unused axes and the YZ/XZ translate planes so Move cannot change Z; XY plane stays), snap, drag leases / undo coalesce, and `hitTest` blocking camera look. Prefab `present: "rtt"` maps CSS coords into Engine pick space and forwards pointer down/move/up into that Scene (`gizmos.forwardPointer`) so UtilityLayer `PointerDragBehavior` runs — the blit canvas is not the Engine input element. A gizmo pointer-down also suppresses the tap pick on pointer-up (`skipTap`), so releasing a handle does not `selectActor(null)` when the utility-layer gizmo is not in the main-scene pick. Prefab shares the same host. Selection outline in `selection-outline.ts` stays a cheap mesh outline (color ~`(0.42, 0.78, 1)`, width `0.022`) — not a HighlightLayer. **Multi-select:** outline covers every selected visual; the gizmo attaches to the first pickable **selection root** (a selected actor whose parent is not also selected). Translate / rotate / scale copies the attached mesh’s world-space TRS delta onto the other roots so relative offsets stay put. Selected children of a selected parent are skipped (the parent mesh already moves them). Prefab has no actor multi-select.

## EditorSceneSync

`EditorSceneSync` applies scene document edits **incrementally** to the Babylon editor scene (reuse meshes, update transforms, rebuild when the visual-part fingerprint changes, despawn removed actors, re-parent). A single identity visual stays one mesh named `editorActor:<id>`. Two or more visuals, any non-identity component `transform`, a Light / Camera / Audio / Particle / **RigidBody** billboard, **or** a `ColliderComponent` dash visual, spawn a **non-billboard** origin root at `actor.transform` plus child meshes `editorActor:<id>|<componentId>`. The origin is a volumetric pick collider (`visibility = 0`, `isVisible` follows the actor, `editorPickProxy`) — Babylon’s default pick skips `isVisible === false` but still hits `visibility === 0`. Billboard icons stay children for drawing (`BILLBOARDMODE_ALL`, double-sided). Collider dashes are world-group opaque segments (`createColliderVisualMesh`, full local TRS via `applySerializedTransform`); they are **not** a UtilityLayer / `EditorDebugOverlay` / UI-group overlay. Mesh / Sprite / Tilemap / Skybox / 3D Text still win over a RigidBody billboard; collider dashes do **not** hide that billboard. The Scene gizmo attaches to that actor origin; outline uses the visual children (pick proxies stay off the outline) so a camera-facing icon cannot overwrite `actor.transform.rotation`. Tap pick (`pickAtCanvas`) refreshes camera-dependent world matrices before `scene.pick` so billboard AABBs match the last draw. Instantiated glTF children keep names like `torso`; picking walks parents to `editorActor:<id>` (same as Play walking to `actor-N`). The empty Model root is unpickable; lock/visibility copy onto descendants. Collider dashes stay `isPickable = false`. Picking a child or the origin collider still selects the actor — components are not independently movable in the Scene viewport. Nested `actor.parentId` parents origin roots; the child actor origin is the attach point. Each apply binds `MeshComponent.materialGuid` onto the matching visual (and imported-model descendants) via `resolveMaterial`; `materialGuid` is not in the visual fingerprint. Empty guids restore the construction material. `meshKind: "pivot"` markers stay unlit. `SkyboxComponent` is a visual (`createSkyboxMesh`); the fingerprint includes `size` and the six face Texture guids. `Text3DComponent` is a visual (`createText3DMesh`). The skybox mesh stays `isPickable === false` even when the actor is unlocked. Shadows skip it.

`setMeshAssets` fingerprints sprite/tilemap/tileset guids, texture/model byte sizes, and `pixelsPerUnit`. Equivalent payloads (new `Map` instances after a transform commit) are a no-op so the live mesh is not disposed. Disposing it would leave the gizmo and outline bound to a ghost mesh, and the next drag would only jump on submit. When **only** `modelBytes` / model payloads appear, instantiations attach to existing named roots — the scene is not torn down. When the rest of the fingerprint actually changes (first textured load, swapped sprite), meshes rebuild and `createEngine` re-applies the last `setSelectedActors` ids so the gizmo attaches to the new mesh. `setMaterialDocuments` updates the library and re-applies the last scene so Prefab's async collect can bind materials after `loadScene` without a mesh rebuild. Browser parity tests inspect the live Babylon materials in both Scene and Prefab viewports, including the unlit Prefab Root exception.

Invalidates the render scheduler on apply (`"asset"` reason). Viewport wiring also invalidates on **`camera`**, **`gizmo`**, and **`selection`** (see [render.md](render.md)).

Gizmo drags coalesce via `SetActorTransformCommand.mergeKey` (`transform:{actorId}`) for one actor, or `SetActorsTransformsCommand.mergeKey` (`transforms:{sortedIds}`) when two or more selection-root transforms change — one undo step per gesture. Viewport `commitGizmoTransform` only runs when `onGizmoDragStart` stored a scene; a Babylon `onDragEnd` without a drag (attach/rebuild) must not write live mesh TRS and re-dirty after Save All.

## Packages

| Package | Responsibility |
| --- | --- |
| `@babylonslate/core` | `SerializedScene`, `SceneSettings`, `ViewportMode`, normalisation |
| `@babylonslate/edit` | Scene commands + `diffSceneCommands`; journal revivers |
| `@babylonslate/render` | Editor tools: camera, gizmos, grid, outline, sync, gestures |
| `@babylonslate/editor-kit` | Property grid, tree view, panel frame, toolbar, asset picker, class picker, input mapping editor |
| `apps/editor` | Viewport, Outliner, Details, Place Actors catalog, Actor prefab tab; `applySceneChange`. **Windows** (global toolbar, left of Focus) lists Viewport, Outliner, Details, Output Log, and **Compiler Results** (tabbed `within` Output Log). Reopen restores addPanel-relative last placement. UserInterface **authoring** is a Dockview document (`p12-ui-editors`). |

## Outliner and Details visuals

- **Outliner** (`TreeView`): `--chrome-row` (28px) rows, type icon from `resolveActorTypeVisual` (Actor color; mesh/light/camera glyphs for engine `Actor` placeholders; user classes use the parent engine icon), selected row `bg-primary/20` + `border-l-primary`. **Search**, **New Folder**, and **+** share one row. Rows are namespaced `folder:<id>` / `actor:<id>` (`flattenOutliner`, `outlinerRowTarget`) so a folder can never be mistaken for an actor; folders sort above the actors they hold. Visibility/lock are compact ghost `IconActionButton`s (eye / eye-off, and unlock / lock so both states are visible). Immediate drag-to-parent (`reparentArm: "immediate"`); drop on the **middle** of a row attaches as child; the **top/bottom 8px** insert as a sibling of that row (1px line, not a row outline) via `outlinerTreeDropMoves` `placement`. A selected in-tree drag moves collapsed folder/actor roots (`outlinerTreeDropMoves`) so parent+child picks cannot double-move. Dragging an actor row **outside** the tree onto the scene viewport duplicates that one actor (`duplicateSceneActor`: new id, `{name} Copy`, cloned components/TRS flags, `parentId` cleared so the copy is not stuck in the source local space, `folderId` kept; children are not cloned) at the drop world position (`worldPositionAtClient`). `GraphDropHint` (`outliner-drop-hint`) shows `+{name}` over the canvas and a ban elsewhere; folders cannot drop. In-tree drops keep reparent/group. Trailing **⋯** opens Open Actor / Duplicate / Delete (`NestedMenu`) — Open Actor is shown when `actor.classId` maps to a project Class asset (`classIdFromClassAsset`) and opens that graph document; engine `Actor` and classes with no asset omit it. Duplicate uses the same helper without a new position. **Double-tap** frames the actor (`frameActor` via viewport context): in 3D it moves the look-at / orbit center to the actor origin and keeps the camera eye; in 2D it pans the target. Double-tap on a skybox is a no-op (`infiniteDistance` tracks the camera). **+** opens a **Place Actors** `CatalogDialog` (Shapes, Lights, Camera, Empty, **Audio**, Environment → **Skybox** / **3D Text**, **Particle**, Project assets) with the same type icons. Its search and category reset whenever the dialog closes — the Outliner closes it itself after spawning, so resetting from the dialog's own `onOpenChange` missed the common path. Spawned actors sit at the **center of the editor viewport**, a short distance along the view ray (`worldPositionAtViewCenter` / `editor-place.ts`: 3D view-facing plane at `max(4, radius * 0.75)` so they are neither in the camera lens nor swallowed by the origin Cube; 2D maps onto XY at Z = 0). An unmounted viewport falls back to the origin. **New Folder** adds an Outliner folder, nested inside the selected folder when there is one. Spawned lights create a Babylon `PointLight` / `DirectionalLight` / `SpotLight` (`authoredLight:<actorId>`) with authored color / intensity / range / cone and dim the default hemi; direction is actor rotation × `(0,0,1)`. Spawned cameras create a detached `UniversalCamera` (`authoredCamera:<actorId>`). Place Actors / Add Component cameras keep `attemptPossessViewTarget: false`. **New scenes** (`createDefaultScene`) seed a Camera with possess on and Default Camera ids set: 3D posed like the editor orbit (`alpha = -π/2`, `beta = π/2.5`, radius 8) with identity rotation (looks along world +Z); 2D orthographic at `(0, 0, -8)` with identity rotation. Cube stays `actor-1`. **3D also seeds** a locked Skybox (`actor-skybox`, empty faces → engine default cubemap) and an unlocked Directional Light (`actor-sun`, warm color `[1, 0.96, 0.88]`, intensity `1.5`, `castShadows: true`) plus a horizon-blue `environmentColor` so deleting the skybox does not flash near-black. 2D stays camera-only. Place Actors → Environment → Skybox can add another (multiple allowed; no uniqueness gate). Delete uses normal `RemoveActorCommand`. The editor orbit camera stays ArcRotate (`stealActiveCamera: false`) unless Viewport Settings **Game Camera** preview is on. Play looks through the named Default Camera (`mainCameraActorId` / `mainCameraComponentId`); missing keeps the Play default camera. Light, camera, audio, particle, and **RigidBody** actors also use camera-facing billboard icons instead of the cube proxy so they stay pickable (billboards are icons, not the illumination system — the icon is a child of a non-billboard origin that is a hidden volumetric pick collider). Place Actors copies class prefab `SerializedGraph.components` from the open tab **or** the disk class document.
- **Details** (`PropertyGrid`): stacked **title-above-control** rows (`Field` vertical) with compact inspector spacing. Labels are Title Cased (`meshKind` → `Mesh Kind`, `2D camera width` → `2D Camera Width`). Category headers use `--secondary`. Vector axes use `--axis-x/y/z` on one nowrap row under the title (letters live on `NumericDragField`). In **2D**, Position is X/Y only; `transform.position.z` is a separate **Z-Order** number (same stored value as 3D Position Z; not on the Move gizmo). Rotation is Z only; Scale is X/Y. 3D keeps Position/Rotation/Scale XYZ. Scalar number rows keep a compact unlabeled scrub handle so the title is not repeated. Checkboxes are compact (`size-4`) under the title. Reset is a compact icon button (`↺`) on the title line, not the word Reset. Controls use `--chrome-row` (28px). **Add Component** uses the same catalog chrome as the prefab Components tab: engine types (Rendering including Skybox / **3D Text** / Animation / AI / Camera / Audio / Particles / Physics) plus a **Project** category (Model binds `MeshComponent.assetGuid`; Audio / ParticleSystem / Sprite / Tilemap / AnimationGraph / BehaviourTree bind the matching guid; user `ActorComponent` classes). Inspector **Asset** pickers still work after add. Numeric rows (`NumericDragField`) keep an empty typed draft; emptying does not commit `0` — blur restores the last committed number. Idle display rounds to two decimals without changing the stored value; tap to type arithmetic (`1+2`) or a leading `*` `/` `+` relative to the current value (`*2`). Focusing a value field (number, vector axis, text, hex) selects all on the first click, tap, or Tab so typing overwrites; a second gesture while focused places the caret.
- **Typed component rows** (`apps/editor/src/lib/component-property-rows.ts`): `MeshComponent.assetGuid` / Sprite / Tilemap / Widget / AnimationGraph / **AudioComponent** `audioAssetGuid` / **ParticleComponent** `particleSystemGuid` open `AssetPicker` (closed button shows icon, name, and type; stores the guid). `AudioComponent` also exposes play-on-start, loop, and component volume. `ParticleComponent` also exposes play-on-start and sorting layer/order. Sprite/Tilemap `sortingLayer` is an enum from project `twoD.sortingLayers`. `RigidBodyComponent.motionType` is an enum; `gravityScale` / damping use sliders (0–10). `ColliderComponent` in Scene Details also shows a **Transform** grid (`data-testid="collider-transform-grid-<id>"`; Prefab Inspector already has component TRS). Viewport gizmo stays on the **actor** origin. Shape flattens to kind plus half-extents / radius (polygon / mesh point clouds are not JSON text); `friction` / `restitution` are 0–1 sliders; **Layer** is a named Select from `settings.physics.collisionLayers` (single bit; Default → `1`); **Collides With** is a `FlagsField` of those named bits only; **Is Trigger** and **Render In Game** are typed booleans (`renderInGame` default false). `LightComponent.color` is a native color row; `intensity` is a 0–16 slider; `lightKind` is Point / Spot / Directional; `enabled` / `castShadows` are booleans; `range` defaults to 10 (hidden on directional); Spot also shows `innerAngle` / `outerAngle` (degrees). Camera FOV (1–179, default / Reset **60**) and orthographic size (0.1–50, default / Reset **5**) are sliders (`DEFAULT_CAMERA_FIELD_OF_VIEW` / `DEFAULT_CAMERA_ORTHOGRAPHIC_SIZE`); both stay visible so Reset works without switching projection mode first. `projectionMode`, `nearClip`, and `farClip` are explicit. **Attempt Possess View Target** (default off) asks Play to possess that camera once the scene is realized; see [engineplan §2.5](../engineplan.md). `SkyboxComponent` exposes a **Size** slider (default 1000) and six Texture `AssetPicker` rows labeled **Positive X** … **Negative Z** (`px`/`py`/`pz`/`nx`/`ny`/`nz`); empty = engine default cubemap. `Text3DComponent` exposes **Text**, **Size**, **Depth**, **Color**, and Font (`allowedTypes: ["Font"]`); a disabled **Typeface** row notes bundled ASCII when the Font has no facetype chunk. Scene empty-selection Details include Fog (Color / Start / End only when `fogEnabled`), IBL `environmentTextureGuid` (not the skybox mesh), and **Default Camera** via kit `SceneComponentPicker` (`CameraComponent` only; button shows icon, actor + component name, and `CameraComponent`). Scene **Game Instance** is a read-only pointer to `project.json` `gameInstanceClass` (Project Settings → Export `ClassPicker`, GameInstance lineage; closed button shows icon, class name, and **Class**). On load, an empty project field copies the first scene value once so old projects keep Play.

- **Viewport overlay**: toolbar island on `--popover` with a shadow so it separates from the 3D view. Move / Rotate / Scale (the enabled gizmo shows its name; inactive labels stay mounted at `grid-template-columns: 0fr` so width eases to `1fr` with no reserved empty space), **Drag Select**, a **Viewport Settings** `NestedMenu` (Snap, Show Grid, Joystick, **Pivot Around Center**, **Game Camera**), and 2D/3D. Gizmo / Drag Select / Focus pressed states use accent fill + primary border. Drag Select hijacks the next one-finger gesture in 2D and 3D (live dashed overlay `viewport-marquee`), then unpresses and keeps the multi-selection. A gizmo drag then moves the whole selection as a group. Prefab hides Drag Select. Selecting a camera actor draws a local 12-edge dashed frustum parented to the camera origin (FOV / clips / composed rotation; follows gizmo drags) and a 1 fps corner preview (`camera-preview`). Selecting a light actor draws dashed influence (point rings, spot cone, directional arrow). Collider dashes are **always** drawn as world meshes (not selection overlays). Scene **Compiler Results** (Output Log tab) lists physics pairing warnings; tap selects the actor. Pairing warnings do **not** block Play. Opening or remounting the scene viewport (idle-unmount, first open) shows an uncancelable **Loading Scene** dialog (`scene-loading-dialog`) with determinate Progress until collected assets are applied and `whenEditorModelsReady()` settles. Incremental `EditorSceneSync.apply` (gizmo / Details) does not open it. Prefab viewport is out of scope.

## 2D specifics

- **Marquee**: hold ~250ms then drag in 2D selects actors whose origin falls inside the rect (`viewport-gestures.ts`, `two-d.ts`). Hidden volumetric pick colliders (`visibility = 0`) are skipped so a light/audio helper is not selected twice (icon + origin). Immediate one-finger drag **pans** 1:1 with the pointer (orthographic frustum over CSS canvas size, so hardware scaling does not change feel). 3D one-finger drag looks the camera instead. **Drag Select** (toolbar) marquees immediately in both modes using the same origin-in-rect test, then turns itself off.
- **Tile grid**: shader plane on XY (2D) or XZ (3D) in `RENDERING_GROUP.world` with meshes (depth test on, depth write off). Major lines at `settings.grid.tileSize`, minor lines at `tileSubdivisions`. The plane snaps to the editor camera target and scales with the frustum so it does not cut off (`editor-grid.ts`). Edge fade is planar, so 2D zoom-in stays opaque; the grid fades out when the view is far enough that too many cells would shimmer (view fade starts at 28 major cells, gone at 100). `settings.grid.showGrid` toggles it; 2D `cameraBounds2D` stays a separate line overlay.
- **`cameraBounds2D`**: rectangle drawn in the viewport for the game camera frame.
- **`pixelsPerUnit`**: project setting (default 100); drives pixel-perfect ortho bounds and grid snapping.
- **Pixel-perfect sampling**: when `twoD.pixelPerfect` is on in 2D mode, `setPixelPerfect` also runs `applyPixelArtSamplingToScene` (nearest sampling, clamp wrap, anisotropy 1) on every texture currently on the Babylon scene.
- **Integer zoom steps**: project setting (`twoD.integerZoomSteps`, on in the 2D template) does **not** quantize the editor camera. Pinch and wheel stay continuous so 2D zoom tracks the fingers. When `twoD.pixelPerfect` is on, Play snaps the **game** camera to the pixel grid; the editor pan/zoom target stays continuous (§13.5). `pixelZoom()` is the live editor scale.
- **Sorting layers**: ordered list in Project Settings (`NamedListEditor`); `(sortingLayer, orderInLayer)` compiled to one `alphaIndex` sort key; rendering groups reserved for coarse background / world / foreground / UI separation (`sorting.ts`). Actor **Z-Order** is still `transform.position.z` (depth-tested meshes); sorting layers remain the sprite/tilemap draw-order key.
- **Collision layers**: Project Settings → **Physics** (`NamedListEditor`, `settings.physics.collisionLayers`, default `["Default"]`, cap 32). Collider **Layer** / **Collides With** use those names. See [physics.md](physics.md).
- **Audio mixer**: Project Settings category **Audio** picks an optional `AudioMixer` (`audio.audioMixerGuid`, None default). Place Actors **Audio** spawns an actor with `AudioComponent` (billboard icon). See [audio.md](audio.md). Place Actors **Environment → Skybox** spawns a locked actor with `SkyboxComponent` (empty faces = engine default cubemap). Place Actors **Environment → 3D Text** spawns an unlocked actor with `Text3DComponent` (bundled TypeFace until a Font with a facetype chunk is assigned). Content Browser **Skybox Creator** is the editor-only helper that writes the six face Textures; assign those guids on the component. See [engineplan §2.5](../engineplan.md).
- **Particles**: Place Actors **Particle** (and Project Particle System tiles) spawn an actor with `ParticleComponent` (billboard icon). See [particles.md](particles.md).
- **Locked actors**: `isPickable = false` and the viewport gizmo attaches only to pickable meshes. **Locking an actor immediately drops it from `selectedActorIds`** (Outliner lock toggle and Details Locked). Unlock does not reselect. Details then shows scene settings when nothing remains selected. The seeded 3D Skybox is locked so it is Outliner-only until unlocked. The skybox **mesh** stays `isPickable === false` even after unlock (viewport clicks must not hit the cube interior).

## Actor Prefab tab

Prefab is a **window of the class document**, not a fourth chrome `DocumentKind`. Default class layout:

| Dock           | Panels                                                                                                                                                                                                                                                                          |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Center group   | **Graph** and **Prefab** as siblings (`direction: "within"`). Selecting Prefab fills the workspace like Viewport does on a Scene.                                                                                                                                               |
| Left           | **Components** above **Class** (My Blueprint member tree with type-colored rows and trailing section +). Inspector shows the selected **component or** class member (component wins when a real component is selected; Prefab Root / empty space keeps member or node details). The Components tree uses the same TreeView multi-select as Outliner (Ctrl/Shift/Meta, swipe add, two-finger range). Additive on Prefab Root exclusive-selects Root. Viewport tap stays exclusive. Inspector shows **N Components** (no property grid / Prefab Origin) when more than one component is selected. Remove deletes every selected local component (and descendants). Immediate drag-to-parent moves collapsed selection roots when the drag id is selected (`reparentPrefabComponents`); drop on a row middle nests, and the top/bottom 8px insert as a sibling (Prefab Root before/after still unparents). A cycle on any root no-ops the drop. |
| Right / bottom | Inspector, Compiler Results                                                                                                                                                                                                                                                     |

The Prefab viewport reuses `ViewportToolbar` + `createEngine` (unlit gizmos, fly/look camera, settings menu for snap / show-grid / joystick / **Pivot Around Center**) with `present: "rtt"`. Gestures bind to the 2D blit canvas; gizmo drags are forwarded into that Scene in Engine pick space. Drag Select is hidden — Prefab has no actor multi-select. World `(0,0,0)` in this viewport **is** the Prefab Root origin, which **is** `SerializedActor.transform` after Place Actors / Play (and the attach origin when that instance is a child actor). An always-visible `meshKind: "pivot"` marker (unlit RGB axes + center sphere, engine debug geometry) sits there; tapping it selects Prefab Root. **Double-tap** Prefab Root or a component row in Components frames that preview actor the same way Outliner frames a scene actor (keep-eye retarget in 3D). Preview meshes show authored `MeshComponent.materialGuid` materials from the collected Material library (same `EditorSceneSync` bind as the scene viewport), not only Babylon default primitives. Each component is its own pickable preview actor, so a viewport tap or tree selection gizmos that piece independently. Inspector shows Position / Rotation / Scale for a selected component (Euler degrees in the UI, quaternion on disk; 2D adds **Z-Order** for `position.z`). Scene Details shows that Transform grid on `ColliderComponent` only. Physics pairing warnings appear in this document’s Compiler Results (same codes as the Scene tab). Dragging Prefab Root bakes the inverse TRS into root-level (`parentId == null`) component locals and snaps the marker back to the origin (Unreal-style Edit Pivot). Nested component locals stay put. The Scene viewport does **not** draw those pivot axes — the actor gizmo is that origin. Component property edits in Inspector go through `updateComponent` / `updateComponentTransform` → `applyGraphChange`. The canvas is full-size, not a 160px sidebar strip. Prefab Preview keeps the near-black studio clear (`createDefaultSceneSettings().environmentColor`) and does not inherit the default 3D skybox actor or horizon-blue `environmentColor`; Scene viewports still do. An authored `SkyboxComponent` on the prefab still previews as its own actor. Component add/remove/reparent (`parentId`) writes `SerializedGraph.components` through `applyGraphChange` (`graph.setComponents` / `scene.reparentComponent`). Place Actors instantiates those components (including `transform`) when spawning a Class asset from an open tab **or** from the class file on disk.

**Focus** (toolbar toggle) closes dock tabs that are not on the Engine Settings Focus keep-list. It is available on every DockView document kind (`isDockviewDocumentKind`), not only Scene/Class. Defaults include Scene Viewport, Class Graph, Material Graph, UserInterface Design (or Logic Graph), Animation Graph State Machine Graph (or Animation Object Graph), Behaviour Tree Graph, **Skeleton Preview**, **Animation Preview**, and **Skybox Creator Preview**. Keep-listed tabs stay only if they were already open. Saved `layout.json` is restored with `fromJSON`; a throw from an older catalog falls back to the kind’s default DockView layout instead of crashing the editor.

Play instantiates the **open scene tab** (`playSceneFromOpenDocuments` / `resolvePlayScene`) when **Preview Build is off**. Graphs and Content Browser may stay open; **only one scene document** is open at a time (opening another closes the previous after the Unsaved dialog). The open scene tab is pinned immediately to the right of Content Browser so it stays visible while other asset tabs scroll. **If no scene tab is open, overlay Play is disabled** — it does not fall back to `startupSceneGuid`, `assets/main.scene.babasset`, or demo actors. The open scene path is resolved to its registry asset guid before the runtime `load`, and the overlay scene library uses the same canonical guids as Preview Build/export so `activeScene` works identically. `startupSceneGuid` on `project.json` is the packaged / export / **Preview Build** boot scene (asset guid, so rename is safe). **Preview Build** (Debug dropdown, default off) does not require a scene tab; missing or stale startup shows `Set Startup Scene in Project Settings.` Overlay Play and Preview Build never share an Engine — Preview Build uses a dedicated player iframe. See [exporter.md](exporter.md). Play looks through the named **Default Camera** (`SceneComponentPicker`, `CameraComponent` only). Missing, None, or a stale id keeps the default Play camera. New scenes already pick that Default Camera and set **Attempt Possess View Target** on it so overlay Play starts in the authored view. **Possess Camera** switches that global Play camera from a Class graph. See [physics.md](physics.md) and [render.md](render.md).

See [command-layer.md](command-layer.md) for undo/journal and [gestures.md](../design/gestures.md) for touch contracts.
