# Editor UI components

Reusable Editor chrome lives in `@babylonslate/ui` (shadcn / Base UI primitives) and `@babylonslate/editor-kit` (touch-first composites). Graph canvases reuse `@babylonslate/graph-ui`. Do not add raw styled `<input>`, `<select>`, or `<button>` in `apps/editor/src` — compose from this list.

Tokens and action-vs-pressed rules: [theming.md](theming.md). Spec: [engineplan.md](../engineplan.md) (UI composition). Dev-only visual audit: `/?test=1&gallery=1`.

This page lists **kit** components currently in the repo. Feature screens (Homepage, Play overlay, Settings modals, dock panels) are not listed. When you add, change, or remove a reusable component, update this page in the same change (`.cursor/rules/editor-ui-components.mdc`).

**Asset document shells** are DockView (`DockviewShell` + `window-catalog.ts`), not shadcn `Tabs` and not a new `AssetDocumentWorkspace` page. Register every panel so the toolbar **Windows** menu can show, hide, and restore it. Animation Graph adds a chrome **State Machine | Animation Object** mode bar outside DockView (see the exception in the rule). Rule: [`.cursor/rules/dockview-editor-tabs.mdc`](../../.cursor/rules/dockview-editor-tabs.mdc). Compose each dock tab from this catalog (`PanelFrame`, `PropertyGrid`, …).

## Primitives (`@babylonslate/ui`)

Source: [`packages/ui/src/components/`](../../packages/ui/src/components/). Import as `@babylonslate/ui/components/<name>`. Composition parts (`DialogTitle`, `FieldLabel`, …) belong with the family, not as separate rows.

| Component | What it does | Used for |
| --- | --- | --- |
| **Alert** (`AlertTitle`, `AlertDescription`, `AlertAction`) | Inline status callout. | Boot / project errors in `App` and Homepage; document tab `WorkspaceErrorBoundary`; gallery. |
| **AlertDialog** (`AlertDialogTitle`, `AlertDialogAction`, `AlertDialogCancel`, `AlertDialogMedia`, …) | Modal confirm / blocking error. `variant="destructive"` is the danger chrome (red ring, red media well, red title) for irreversible file deletes. | Unsaved close, Play blocked, Content Browser and plugin delete confirms; NamePromptDialog; Content Browser **Retarget Failed** (`content-browser-retarget-errors`); gallery. |
| **Badge** | Compact status chip. | Compilation errors, Content Browser type tags, global-search result kinds, live-wire “Tap to Cancel” / “Release to Add Node”, Class/Prefab **Inherited** markers, graph member drop hint. |
| **Button** | Pressable action. Sizes include `touch` / `touch-icon` (44px). Outline = visible action; ghost = tabs / close. `destructive` is a **solid** filled `--destructive` control for irreversible confirms, not a 10% tint. | Chrome (including global Material Render), panels, catalogs, overlays; Content Browser / plugin delete confirms; Homepage Create Project **App Documents** / **Choose Location…** (native). |
| **Card** (`CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`, `CardAction`) | Raised content block. | Homepage Start template cards and recents rows; settings field groups. Compiler Results diagnostic rows are windowed `Button`s (`data-testid="compiler-result-row"` on **mounted** rows), not Cards. |
| **Checkbox** | Boolean check control. | PropertyGrid booleans; gallery. |
| **ContextMenu** (`ContextMenuTrigger`, `ContextMenuItem`, `ContextMenuGroup`, …) | Pointer-anchored menu (Radix/Base UI). | Gallery only. Homepage recents and editor long-press menus use editor-kit `ContextMenuOverlay`. |
| **Dialog** (`DialogTitle`, `DialogHeader`, `DialogContent`, …) | Centered modal. Title required. Close control is `touch-icon` (44px) on the existing `DialogPrimitive.Close` path. | Global search, Play prepare, Content Browser rename/move/New Asset, CatalogDialog shell, SearchDialog, AddFunctionDialog, Play debug console overlay, Play inspector overlay, Preview session report, navmesh bake; Class member Get/Set chooser after dropping a variable onto the graph. |
| **DropdownMenu** (`DropdownMenuTrigger`, `DropdownMenuItem`, `DropdownMenuGroup`, …) | Anchored menu. `DropdownMenuLabel` must sit in a `DropdownMenuGroup`. Chrome menus use `modal={false}` so they do not cover the viewport. | Windows, Debug, Settings, Add, Content Browser item menus and Filter/Sort, Homepage Projects Filter/Sort, SearchDropdown. |
| **Empty** (`EmptyHeader`, `EmptyTitle`, `EmptyDescription`, `EmptyContent`, `EmptyMedia`) | Empty-state layout. | Homepage with no projects; global search with no hits and while indexing (`global-search-pending`); Tileset Preview with no Texture; Tilemap Paint/Palette with no Tilesets; Play inspector with no selection; Model Preview when GLB bytes cannot load; gallery. |
| **Field** (`FieldGroup`, `FieldLabel`, `FieldSet`, `FieldLegend`, `FieldDescription`, `FieldError`, `FieldContent`, `FieldTitle`) | Form row layout. Uses **Label** internally — do not invent a parallel label. Horizontal rows with a description wrap the copy in `FieldContent` so a compact control can sit on the right. | Engine / Project Settings (including Viewport **Post-processing** is `FieldContent` + compact Switch), Homepage create, Content Browser forms, PropertyGrid (vertical Details and horizontal Play inspector). |
| **Input** | Single-line text. | Names, search fields, settings strings. |
| **Label** | Accessible control caption. | Not imported by app code; Field wraps it. |
| **Progress** (`ProgressTrack`, `ProgressIndicator`, `ProgressLabel`, `ProgressValue`) | Determinate progress. | Content Browser encode / import; Preparing Preview modal; uncancelable **Loading Scene** dialog on viewport remount / first open (Collecting Assets → Loading Models → Warming Shaders). |
| **ScrollArea** (`ScrollBar`) | Overlay scrollbar region. Docked panels may use `min-h-0 flex-1`; compact pickers use a **definite** native overflow height instead of ScrollArea so coarse-pointer pan is not cancelled by the document overscroll guard. | Gallery, Output Log, Compiler Results. Output Log / Compiler Results pick lists window via `WindowedList` (`windowedSlice`; render-all when measured height is 0). Place Actors catalogs stay unwindowed. Global Search **result** body stays unwindowed. |
| **Select** (`SelectTrigger`, `SelectContent`, `SelectItem`, `SelectGroup`, …) | Closed option list. Popup is at least the trigger width (`min-w-(--anchor-width)`), not stretched across the panel. | Engine Settings enums; PropertyGrid enum rows (Material Details **Domain** Surface / Post Process / Particle); Project Settings global font fallback. |
| **Separator** | Horizontal or vertical rule. | Toolbar strips, search dialog, catalog layout. |
| **Sheet** (`SheetTitle`, `SheetContent`, `SheetHeader`, …) | Edge drawer. Title required. | Installed; unused in production chrome. Pick lists use SearchDialog / SearchDropdown; console and session report use Dialog. |
| **Slider** | Bounded numeric track (44px hit area). Accepts a scalar or range array. | PropertyGrid slider rows; Trace playback frame scrubber; Sprite Animation Preview; gallery. |
| **Switch** | On/off toggle. Thumb already has a padded hit area — do not stretch the 18px track to `--touch-target`. | Engine / Project Settings booleans (including Viewport **Post-processing**); NodePalette **Context Sensitive**; scene post-process pass Enabled. |
| **Textarea** | Multi-line text. | Gallery; `MarkupAutocompleteTextarea` and `MultilineTextField` host it. Input mappings stay on structured `InputMappingEditor`. |
| **Toggle** | Pressed/unpressed tool. Selected = accent fill + primary border + `aria-pressed`. | Viewport tools, Outliner filters, chrome Play-adjacent tools; FlagsField bits; Sprite Animation Preview **Loop**; Animation Preview **Show Bones**; Model Preview **Show Collision** (session, default on). Scene/Prefab Mesh dashes use Viewport Settings **Show Collisions**, not this Toggle. |
| **ToggleGroup** (`ToggleGroupItem`) | Exclusive (or multiple) tool set. 44px `touch` size for document tools. | Viewport gizmo mode; Material preview mesh; Model Preview collider **Move \| Rotate \| Scale** when a collider is selected; Tilemap paint tools (**Move** default, then brush/eraser/rect/bucket/stamp/picker); Tileset Preview **Move \| Select**; Animation Graph **State Machine \| Animation Object** mode bar; Class variable **Single \| Array \| Map** container; gallery. SceneLayer viewports hide the 3D/2D Viewport Mode `ToggleGroup` (`showViewportModeToggle={false}`; locked `"2d"`). |
| **Tooltip** (`TooltipTrigger`, `TooltipContent`, `TooltipProvider`) | Hover/focus hint. `TooltipProvider` wraps the editor in `App`. | Icon chrome, viewport toolbar, `IconActionButton`. |

Gallery-only today (installed, not yet used in production panels): **Skeleton** (loading placeholder — not the Skeleton **asset** document). **Tabs** (`TabsList`, `TabsTrigger`, `TabsContent`) is used inside some asset workspaces (Font; nested modes). Sprite / Sprite Animation / Tileset / Tilemap / Material / Material Function / Enum / Structure / ScriptInterface / Class / Animation Graph / Behaviour Tree / **Model** / **Skeleton** / **Animation** / Particle Emitter / Particle System / Skybox Creator / **Trace** document shells are DockView, not shadcn Tabs. Animation Graph **State Machine | Animation Object** is `AnimEditorModeBar` (`ToggleGroup`), not Tabs.

## Composites (`@babylonslate/editor-kit`)

Source: [`packages/editor-kit/src/`](../../packages/editor-kit/src/). Import from `@babylonslate/editor-kit`.

| Component | What it does | Used for |
| --- | --- | --- |
| **PanelFrame** | Docked panel shell (`--sidebar` body, optional title/toolbar). Omit the title when Dockview already shows the tab name. | Outliner, Details, Inspector, Graph, Class, Prefab, Output Log, Compiler Results; Animation Graph Variables / Details docks; Sprite / Sprite Animation / Tileset / Tilemap / **Model** (Preview / Colliders / Details) / **Skeleton** / **Animation** / Audio / Particle / Skybox Creator docks. Output Log caps at `OUTPUT_LOG_RING_CAP` (500) via `appendOutputLogLine`; `WindowedList` windows the mounted `output-log-line` rows (`p20-log-virtualize`). |
| **ToolbarStrip** | Horizontal chrome row of tools. | Component Gallery; `AnimEditorModeBar`; intended for panel toolbars. |
| **PropertyGrid** | Typed Details rows: number, vector3 (2–4 axes), boolean, text, enum (trigger shows the option **label**, not the stored value; each option may set `disabled`; `Select` honors `disabled`), color (`ColorField`; `value` may be `null` = unset), slider, flags (`FlagsField`), asset (guid stored; filled button is `PickerIdentity` via `displayLabel` / `displayType` / `visual`; empty stays text-only None). Asset rows wrap `AssetPickerControl` so a square **Open Asset** control appears to the right of the trigger when the guid can open a tab. `orientation` defaults to `vertical` (label above control). `horizontal` is name left / value right (`Field` + `FieldContent`). Reset ↺ is omitted when the row is `disabled` or has no `defaultValue`. Text and numeric value fields select all on first click, tap, or Tab so typing overwrites; a second gesture while focused places the caret. | Scene Details (typed asset / physics / Game Instance / Default Camera; collider **Layer** enum + Transform grid; **2D Text** / **2D Rich Text** Renderer greys out MSDF when the Font has no JSON+PNG pair; Mesh **Collision** Use Simple / Use Complex / No Collision), Inspector (node / Log / Print / Print String / Draw Debug / **Development Only**; Prefab component Position/Rotation/Scale and 2D **Z-Order**; action/axis / enumRef / **classRef** defaults via asset-kind rows + `ClassPicker`; Class variable Default), Play inspector (horizontal, disabled typed rows; no `setVariable`), Sprite / Tileset / Tilemap / Structure settings, Model material slots and Colliders TRS, Skeleton / Animation read-only Model and Skeleton rows, Animation Graph **state / transition Details**. Gallery includes a stacked grid and a horizontal read-only example. Class-panel variables use compact Input/Select rows, not PropertyGrid cards. |
| **ColorField** | Native color swatch plus a pasteable `#rrggbb` field. `value` may be `null` (unset: empty hex; native swatch falls back to `#000000` because `<input type="color">` cannot be empty). Focusing the hex field selects all (same first-activate rule as Details text). | Light color; Inspector color pin defaults; gallery. |
| **FlagsField** | Compact 44px bitmask toggles with named labels (or Layer 0–31 when names are absent). | Collider **Collides With** (`mask`, project `physics.collisionLayers`); Tileset tile flags; gallery. |
| **TreeView** | Flattened touch tree (`--chrome-row` 28px): select, expand, reparent, activate. Gesture distance for add/range is **44px**; row height stays 28px. `onSelect(id, { additive, range })`: Ctrl/Shift/Meta click and a **horizontal swipe ≥44px** (inside the tree) add; a **two-finger tap** range-selects from the current row. Vertical / hold reparent is unchanged. Armed in-tree drags use an 8px top/bottom edge (`TREE_DROP_EDGE_PX`): **before** / **after** draw a 1px `bg-ring` line on that row only (no row outline; the adjacent row is untouched) and `onReparent(dragId, targetId, placement)` inserts as a sibling of the hovered row; the middle of the row still outlines and nests (`into`). `selectedIds` highlights a multi-selection. `reparentArm: "immediate"` starts a parent drag after 8px and may capture on down; `"hold"` (default) waits 250ms before capturing so lists can pan-scroll. `onExternalDrop` (mouse after 8px, touch after the hold) fires when the pointer is released **outside** the tree — Class members use this to spawn graph nodes, and swipe-add is skipped so a horizontal drag onto the canvas still drops. When `onReparent` is also set, a release **inside** the tree still reparents; outside still external-drops (Outliner uses both). `onExternalDragMove` / `onExternalDragEnd` drive the floating drop hint while armed. Rows do not use `touch-none`. Scrollport is `min-h-0 overflow-y-auto overscroll-y-contain`. Disclosure only when `hasChildren`. Trailing controls do not start a drag. Arithmetic windowing uses exported `windowedSlice` (`WINDOWED_SLICE_OVERSCAN` 4; **render-all when measured viewport is 0**). NodePalette body reuses it; Output Log / Compiler Results / SearchDialog use `WindowedList` — do not add a virtualizer library. | Outliner (folder and actor rows, immediate drag-to-parent of the collapsed selection when the drag row is selected, **and** actor drag onto the viewport), **Components** (same `selectedIds` / additive / range / collapsed reparent; Prefab Root additive is exclusive); Content Browser sidebar and Move dialog (hold); Class members (trailing **+** on each section, including **Local Variables** when a function graph is open; drag onto Graph); Play inspector (read-only, no `onReparent`); Model Colliders (`simpleColliders` rows); Skeleton Details inspect-only bone tree. |
| **WindowedList** | 1D windowed spacer list: measures the nearest ScrollArea `[data-slot="scroll-area-viewport"]`, or else the nearest `overflow-y` `auto`/`scroll`/`overlay` ancestor; mounts `windowedSlice` rows (overscan 4; **render-all when measured height is 0**), absolutely positions children at `index * rowHeight`. Slot wrappers use `touch-pan-y` so a pan on a row reaches the scroller. `WINDOWED_LIST_TOUCH_ROW_HEIGHT` is 44. Do not add a virtualizer library. | Output Log (`TREE_ROW_HEIGHT` 28, `output-log-line`); Compiler Results / Material / Behaviour Tree diagnostics (44px); SearchDialog picker body; gallery. |
| **NumericDragField** | Scrub-by-drag numeric (axis accent); tap to type (`inputMode="text"`). Idle / scrubbing shows at most two decimals (trailing zeros stripped; stored value unchanged). Typed drafts accept arithmetic (`1+2`, `(1+2)*3`); a leading `*` `/` `+` applies to the current value (`*2`). Incomplete drafts do not commit. First activate selects all so typing overwrites. Coalesces undo via begin/end. | PropertyGrid number / vector3 / slider / color; InputMappingEditor axis extras; gallery. |
| **NumberField** | Numeric text (`inputMode="decimal"`) that keeps an empty draft; commits in-range values, restores on blur. First activate selects all. | Engine Settings numeric fields; Project Settings autosave, pixels-per-unit, play frame cap; Trace playback frame. |
| **SearchInput** | Text field with a trailing clear control. | CatalogDialog, SearchDialog, SearchDropdown, global search, Content Browser, Content Browser Move dialog, Homepage Projects, Tilemap Palette, Play inspector. |
| **SearchDialog** | Searchable item list in a compact centered Dialog. Each row is `PickerIdentity` (icon + name + muted type line) on a `role="option"` div (`buttonVariants` ghost/`touch`, `touch-pan-y`) — not a native `<button>`, so iPad pan scrolls the list. The list body is `role="listbox"` with native `overflow-y-auto` (`touch-pan-y`, `overscroll-y-contain`) and a **definite** height (`pickerListHeightPx`: one `--touch-target` row when empty, then `itemCount * 44` capped at 16rem / `h-64`). Content-sized dialogs cannot grow `h-0 flex-1` (that pattern is for docked panels). The body is windowed (`WindowedList`, `--touch-target` 44px rows, `search-item-*` on **mounted** rows, `{testid}-body` on the scroller). Place Actors / Add Component / Settings stay on CatalogDialog and stay unwindowed. | AssetPicker; ClassPicker; SceneComponentPicker; gallery. Add Component / Place Actors use CatalogDialog instead. |
| **SearchDropdown** | Searchable `DropdownMenu` (`modal={false}` by default) anchored to a trigger. Scrollable; width is content-sized, not full viewport. Rows use `PickerIdentity`. Consecutive items that share `group` render a `DropdownMenuLabel` section header. Query `stopPropagation` skips **Escape** so the menu still dismisses. | Tilemap paint layer; PinTypePicker; BindingCodePicker; Animation Graph leftover Model **Clip Name**; gallery. |
| **MarkupAutocompleteTextarea** | Multi-line `Textarea` plus a **fixed** `h-64` / `PICKER_LIST_MAX_HEIGHT_PX` (16rem) listbox **above** the field that stays mounted (empty state **No matches**, never collapsed). Unfiltered / caret in plain text lists every BBCode-like tag; clicking a tag inserts at the caret (wrapper insert also writes the close tag). `[` + prefix `startsWith`-filters tags; `[color=` lists VGA + orange names and a hex hint; `[img=` offers **Paste Asset Reference**; a trailing space inside `[img]` / `[wave]` offers `size` / `intensity`. Rows are `role="option"` `touch-pan-y` hosts (`WindowedList`, `--touch-target` 44px), not native buttons, so iPad pan scrolls. | `MultilineTextField` modal for Scene Details / Prefab Inspector **2D Rich Text**; gallery. |
| **MultilineTextField** | Read-only outline trigger (looks like a compact text field) that always opens a large `Dialog` with a tall editor. Optional `markup` hosts `MarkupAutocompleteTextarea` in the modal so suggestions do not cover the text. Done / close commits. | Scene Details / Prefab Inspector **3D Text**, **2D Text**, and **2D Rich Text**; Plugin Description; Material Custom GLSL `body`; gallery. |
| **AssetPicker** | Asset-guid picker on SearchDialog, optional None row and type filter. Modal type line is the asset type (Texture, Mesh, Class); path stays searchable via `group` and is not shown. Names strip a trailing `.Type` suffix (`main.scene` → `main`). | Scene Details mesh / sprite / tilemap / animation-graph rows and **post-process Materials**; Project Settings default font and **Startup Scene**; Font fallbacks; Sprite and Tileset Texture; AnimationGraph clip (**Animation** or Sprite Animation); Content Browser **Retarget…** target Skeleton; Material Texture Sample and custom preview Model; Class variable / PinListEditor / Structure field / Blackboard **Structure** and **Enum** type parameters (plus engine-registry rows); **Model material slots**. |
| **AssetPickerControl** | Closed AssetPicker trigger plus a square **Open Asset** control on the right. A grid layout wrapper owns `aspect-square h-full shrink-0` so width equals the picker row height; the outline `Button` fills that box (`h-full w-full p-0`). Visible only when `AssetOpenProvider.canOpen(guid)` is true (project asset with a document kind; Mesh / None / `engine:` registry ids stay hidden). `{triggerTestId}-open` or `asset-picker-open`. Does not open the search modal. | PropertyGrid asset rows; PinListEditor / VariableTypeFields Structure and Enum type triggers; Settings Font / Mixer / Startup Scene; Inspector / type Details / Blackboard; Material Texture Sample and Material Function; Font fallbacks; scene post-process Materials; gallery Texture demo. |
| **ClassPicker** | Class-id picker on SearchDialog (engine + project Class assets). Modal type line is always **Class** (not Engine/Project). Project Class ids are the compile stem (`main`, not `main.class`). | Project Settings **Game Instance** (GameInstance lineage); Inspector **classRef** pin defaults (subclasses of the pin constraint); Class variable **Class Type** / Class Default; PinListEditor Class Type on object/class pins; Prefab Root **Parent Class**. Scene Details shows a read-only pointer to the project value. |
| **SceneComponentPicker** | SearchDialog picker of components in the open scene; `allowedClassIds` is a source-code filter (no user type dropdown); optional None; closed button and modal row show icon, actor name + component title, and class id. | Scene **Default Camera** (`CameraComponent` only). Reuse for other scene-settings that name a component (light, listener). |
| **NamedListEditor** | Reorderable named string rows (add / remove / up / down, 44px). Optional custom item control or Add-only. | Project Settings sorting layers and **collision layers**; Font `fallbackGuids`; ParameterListEditor enum values; Tilemap layers and tileset list; Scene **Post Process** stack; world Scene Options **Scene Layers** spawn list. |
| **EntryListEditor** | Reorderable typed rows (Add / Remove / up / down, 44px targets). Caller supplies `onCreate` and `renderItem`; no per-row type pickers. Gallery `/?test=1&gallery=1` (`gallery-entry-list`). | Class Inspector Array / Map variable default entries. |
| **AtlasTileGrid** | Pixel-aligned atlas overlay: `%`-positioned cell buttons from `tilesetTileRect`, Full hatch + Chain polyline on the selected cell, `object-contain` image, optional pinch/wheel pan-zoom (`touch-none`; two-finger translation pans about the pinch midpoint), optional **Move** (default when `panZoom`) / **Select** (`tool`; one-finger drag pans in Move, tap < 8px still selects), `onImageSize` from the texture. Cells stay `data-testid="{id}-cell-{tileId}"`. `Empty` when there is no image. | Tileset Preview; Component Gallery. |
| **InputMappingEditor** | Actions/axes as `Card`s: muted header (name, axis Kind), **Bindings** group, device `Select`, searchable `BindingCodePicker`, modifier toggles, contextual axis extras. Device rows use `TypeColorMark` plus a 2px pin-token start-edge bar. | Project Settings Input; gallery. |
| **BindingCodePicker** | Device-scoped `SearchDropdown` of bindable codes (keyboard `KeyboardEvent.code`, mouse, pointer, Standard Gamepad buttons/axes with layout-agnostic names, touch control ids). Sets `modal` so outside clicks close the picker only, not a parent Settings dialog. | InputMappingEditor bindings; gallery. |
| **NamePromptDialog** | AlertDialog + 44px name field. Replaces `window.prompt`. | Class panel variable/event add (including Local Variables); Scene Outliner folder rename. |
| **NumberPromptDialog** | AlertDialog + NumberField seeded with a saved value. | Scene/Prefab island **Grid Size** and **Camera Speed**. |
| **AddFunctionDialog** | Larger Dialog + bounded native overflow list with the same definite `pickerListHeightPx` cap as SearchDialog (16rem). Override rows are non-button `role="option"` hosts (`touch-pan-y`) like SearchDialog. **New Empty Function** plus overridable parent functions and ScriptInterface methods (overwritten rows muted). Optional title / empty / name copy and `data-testid` so Events **+** can reuse it as **Add Event** (native, inherited, or attached-component events; same scroll cap). | Class Functions **+** and Events **+**. |
| **CatalogDialog** | Large centered dialog: category nav, non-autofocused search, scrollable body. | Engine / Project Settings, Place Actors, Add Component, graph NodePalette. |
| **CatalogItemButton** | Full-width outline row for a catalog entry. | Place Actors, Add Component. |
| **TypeVisualIcon** | Colored Lucide glyph for an asset / class family (`resolveTypeVisual`). `size` defaults to 16 (chrome); pass 40 (`TYPE_VISUAL_ICON_TILE_SIZE`) on Content Browser tiles so SVG `width`/`height` match the CSS box, with Lucide `absoluteStrokeWidth` so stroke stays 2 CSS px. | Outliner, Details, Prefab, Content Browser, global search, Place Actors, Add Component, AssetPicker, ClassPicker, SceneComponentPicker, `PickerIdentity`, document tabs, Play inspector tree; gallery. |
| **PickerIdentity** | Icon + truncated name + muted type line. Shared by SearchDialog/SearchDropdown rows and closed picker buttons. Empty/None stays plain text (no fake type). Helpers: `displayPickerTitle`, `assetRowIdentity`, `classRowIdentity`, `selectedPickerIdentity`. | PropertyGrid asset triggers; AssetPicker / ClassPicker / SceneComponentPicker rows; PinListEditor Class Type and Structure/Enum Type; Inspector Class Type / Script Interface / Structure / Enum; Settings Default Font / Startup Scene / Game Instance / Editor Utility Objects; Material / Font fallbacks; gallery. |
| **TypeColorMark** | Swatch + label for a DataTypes color token (`pinColorVar` / `assetColorVar`). | PinTypePicker rows; InputMappingEditor device/section marks; Behaviour Tree Blackboard dock key types; Component Gallery Data Types section. |
| **PinShapeGlyph** | Shared pin/variable glyph: `circle` \| `diamond` \| `list` (three full-width bars) \| `map` (two columns × three rows = key\|value pairs). Hollow until `connected`. Do **not** use Lucide `MapIcon` (NavMesh). | Graph `PinVisual`; Class Variables / Locals; VariableTypeFields Array / Map ToggleGroup. |
| **PinTypePicker** | Compact `SearchDropdown` of pin kinds with `TypeColorMark` (bool, int, float, string, enum, vec2/vec3/vec4, rotator, quaternion, color, transform, object, actor, class, asset, struct, wildcard). Function inspectors also pass **exec**. Array/Map are **not** picker types. | Structure field type; Class variable **Type** / Map **Key Type**; PinListEditor rows (function pins, custom event outputs); Blackboard key type. |
| **VariableTypeFields** | Type `PinTypePicker` plus exclusive **Single \| Array \| Map** `ToggleGroup` (`data-testid="inspector-member-container"`). Array / Map items show `PinShapeGlyph` (`list` / `map`) so Container matches graph pins. Map shows a second **Key Type** picker (and Key Class Type / Key Structure\|Enum Type when needed). Class Inspector order is **Name**, then these type fields (plus Class Type / Asset Type / Structure\|Enum Type), then Default (`PropertyGrid` or Array/Map `EntryListEditor`). | Class Inspector variable Details (member variables and function locals). Gallery `/?test=1&gallery=1`. |
| **PinListEditor** | Compact Unreal-like pin rows (color chip + name + type picker + up/down + trash `IconActionButton`). Object/actor/class rows add a Class Type `ClassPicker` whose trigger is `PickerIdentity`. Asset rows add an **Asset Type** `SearchDropdown` (`ASSET_REF_PICKER_TYPES`). Struct/enum rows add a Structure/Enum `AssetPicker` when `typeAssets` is passed; Execute JavaScript still uses `NamedListEditor` for free-text enum values when it is not. Switching to struct/enum **keeps** `typeClassId`. Optional in/out add buttons. Optional/default/enum details on the selected row. `readOnly` locks names/types and hides add/move/remove. Rows stay `--chrome-row` 28px. | ScriptInterface method pins; Class function **Inputs** and **Outputs** (two editors, exec included; locked for interface implementations); custom event **Outputs** (data types only). |
| **ParameterListEditor** | Thin `PinListEditor` wrapper restricted to string/float/int/bool/enum. | Inspector Execute JavaScript Inputs/Outputs; Event On Command Run / BDebugCommand. |
| **SelectableText** | Opt-in selectable span inside a `user-select: none` shell. | Logs, compiler messages, Play overlay copy, Play inspector tick, debug console transcript, gallery code snippets. |
| **NestedMenu** | Data-driven dropdown or pointer-anchored menu with recursive submenus (actions, checkboxes, exclusive **radio-group**s, separators, labels). Submenus open on tap, not hover-only. Optional `contentClassName` (viewport island: `w-max min-w-56 whitespace-nowrap` so labels stay one line). Chrome submenus use the same nowrap width. Pointer-anchored `OverlayMenu` measures the panel and **clamps** to the viewport (8px margin); overflowing submenus open to the left of the parent. | Windows; Outliner actor row ⋯ (Open Actor / Duplicate / Delete) and folder row ⋯ (Rename / Delete); viewport settings (**Viewport Mode** radio submenu, Snap, Show Grid, **Show Navmesh**, **Show Collisions**, Joystick, Pivot Around Center, Game Camera, **Settings** → Grid Size / Camera Speed); Model Preview **Preview Shading** radio dropdown (top-right, `size="sm"` outline, session PBR / Unlit / Wireframe); Model Colliders **Add**; gallery. Long-press sites use it through `ContextMenuOverlay`. |
| **ContextMenuOverlay** | Pointer-anchored menu driven by `useContextMenu` (500ms hold or `contextmenu`). Renders flat or nested `NestedMenu` items. Stays fully on-screen via `clampOverlayMenuPosition`. | Viewport, Content Browser tiles/grid; Class member rows; Homepage recents rows; gallery. Outliner uses a trailing ⋯ `NestedMenu` instead of a row long-press. |

Related hooks (not components): `useContextMenu`, `useHoldDragMenu`, `useSuppressNativeContextMenu`, `useSuppressIosEditingGestures`, `usePreventDocumentOverscroll`. Label helpers: `humanizePropertyLabel`, `formatEventMemberName`, `formatEventTitle`, `formatBindingLabel`, `displayPickerTitle`. Type lookup: `resolveTypeVisual`, `resolveActorTypeVisual`. Picker identity: `assetRowIdentity`, `classRowIdentity`, `selectedPickerIdentity`. History: `documentHistoryHotkey`.

## Graph (`@babylonslate/graph-ui`)

Reusable by script, shader, animation, and behaviour-tree graphs.

| Component | What it does | Used for |
| --- | --- | --- |
| **GraphEditor** | Touch-first React Flow shell (`pinCompatibility` overrides the exact-kind connection rule; material graphs use it so a Float splats into a vector pin): Blueprint node chrome; Development Only and Editor Only hazard-tape footers (`data.__editorOnly`); hollow pin visuals until linked (`data-pin-connected="false"\|"true"`); Array pins `data-pin-shape="list"`, Map pins `data-pin-shape="map"` (`PinShapeGlyph`; color stays the value type); unconnected literal data inputs show a compact read-only default preview (`data-pin-default`, hidden when wired); tap- and drag-to-connect overlay **resolved display types** (`pinWithDisplayType`) so a bound For Each Element can reach Print without rewriting stored `__pins`; **exec** pins multi-connect, **data inputs** replace on a second wire; cancelled pin-drag disconnect; Break Links (selected nodes’ incident wires, or the selected edge/blend rule); marquee (overlay above the canvas; canvas uses chrome `--card`); Add Node. Off-screen nodes (and incident edges) unmount when the wrapper has a measured size (`p18-graph-virtualize`); a later 0×0 (CSS-hidden tab) keeps the last size so virtualisation stays on; a never-measured 0×0 wrapper still renders the full document (jsdom). `focusedNodeId` (search / compiler / Play navigate) and `selectedNodeId` (Inspector / canvas selection) stay mounted so `fitView` and Copy / Delete still resolve them. Mounting uses `selectVisibleGraphElements` only (`onlyRenderVisibleElements` stays off). `sessionViewport` restores pan/zoom after workspace remount and skips the mount `fitView`. Toolbar **+** (`graph-add-node`, outline Lucide `PlusIcon`) opens Add Node; empty-pane double-tap and right-click / long-press do too (XYFlow `zoomOnDoubleClick` is off so double-tap does not zoom). `onPaletteOpenChange` notifies the host so Class / Anim Graph rule palettes can defer injector rows until open. A pin drag into empty canvas shows **Tap to Cancel**; **release** opens Add Node; a **second pointer** while the drag is held cancels the rubber-band (no catalog). In `connectEndMode="add-node"` a pending pin plus empty-pane tap also cancels (double-tap with no pending pin still opens Add Node). Hosts may pass `nodeTypes`, `edgeTypes`, `defaultEdgeOptions`, `onEdgeDoubleClick`, `onEdgeSelectionChange`, `nodesDraggable`, `nodeDragHandle` (stamped onto each XYFlow node as `dragHandle`; XYFlow 12 has no canvas-level prop), `commitPositionsOnDragEnd` (Material graphs commit XY once per drag via `onChange(graph, meta?)` with a per-gesture `transactionId`; select/dimension chrome does not emit; topology still commits immediately), `connectEndMode` (`default` opens Add Node on a far empty-canvas release and keeps safe-zone wire-break; `add-node` opens Add Node even on a short drag and never breaks; `zone-add-node` opens Add Node on a far release, snap-connects when released near a compatible pin including an occupied plate, never wire-breaks, and shows **Release to Add Node**), `replaceIncomingOnConnect`, `uniqueDirectedPairOnConnect` (one visual edge per source→target pair; reconnect updates handles), `canConnect`, `emptyPaneDoubleTapAddsNode`, `toolbarExtra`, attachment selection, `hiddenToolbarActions` (Copy / Paste / Delete / Break Links / Format), `lockNodeDragAxis` (optional sibling-only reorder), `contextMenuItemsForNode` / `contextMenuItemsForAttachment` (pane + node `ContextMenuOverlay`), and `PaletteNode.nodeType`. Hosts may also pass `selectedNodeId` (highlight without `fitView`; selecting a node clears edge selection), `normalizeConnection`, and `connectionMode` (`"strict"` / `"loose"`). Reconciles external `initialGraph` updates (undo/redo, Inspector) without emitting `onChange`; identical graphs, select-only, and dimension-only changes are not re-emitted. `GraphEdge.type` round-trips. `fitView` uses `padding: 0.2` so node pins stay inside the pane, clear of the widened dock sash. | Graph document panel; Material / Material Function / Animation Graph Object / Behaviour Tree DockView hosts (catalog `__pins` hydrated so Add Node is not an empty box). Animation Graph State Machine uses Unreal `anim.state` nodes and `animTransition` badges (CSS triangle, not lucide) on a more open bezier stub; a badge click is exclusive (deselects nodes); Anim Graph persists `AnimState.position`; BT uses parent-child handles, optional `editorPositions`, and tree-mode menus. |
| **GraphDropHint** | Fixed floating Badge while dragging a tree row onto a canvas: `+` when over a valid drop, ban icon otherwise. Optional `label` shows `+Name` (Outliner actors). Optional `testId` (default `graph-member-drop-hint`; Outliner uses `outliner-drop-hint`). | Class members → graph; Outliner actor → viewport; mirrors the live-wire Tap to Cancel hint. |
| **NodePalette** | CatalogDialog of host-legal nodes with role-color chips. Honors the host `pinCompatibility` rule when filtering by a dragged pin, using the pin’s **resolved display type** when present (typed For Each Element includes Print). Context-sensitive filter **ranks** matching Make/Break Structure and Make/Equal/Switch Enum rows (and engine Make/Break Rotator/Color/Transform) ahead of other compatible nodes. Footer **Context Sensitive** switch (`data-testid="node-palette-context-sensitive"`, default ON): pin-drag / pin filter applies only when on. Search and category reset every time the dialog opens. Category counts (including All) come from the **search-filtered** set. The body is window-virtualised (`p18-add-node-virtualize`; viewport plus overscan, `--touch-target` 44px rows via `windowedSlice`). Category sidebar stays unwindowed. Distinct from `p18-graph-virtualize` (canvas). Host `scriptPaletteNodes` injectors (Cast-to-Class / FL Call) generate when Add Node opens (`GraphEditor` `onPaletteOpenChange`) and memoize on class-set / FL / member identity (`p20-palette-generate`). | Add Node from GraphEditor (toolbar +, empty-pane context menu, double-tap, far pin-drag release, Behaviour Tree `connectEndMode="add-node"`, and Animation Graph State Machine `zone-add-node`). |
| **TreeNode** | Behaviour-tree `bt.node` chrome: Title Case title, compact shell, `data-node-role` (`bt-root` / `bt-composite` / `bt-task`), visible exec diamond pins (the `__protected` root omits the parent handle), header drag handle (`.bt-node-drag-handle`), numeric priority badge, reserved last-result row (`data-bt-state`), attached decorator/service rows, long-press node/row menus, running-branch highlight. | BehaviourTree GraphEditor `nodeTypes`. |


## App wrappers

Reusable pieces in `apps/editor/src/components/` that are not one-off screens.

| Component | What it does | Used for |
| --- | --- | --- |
| **AnimEditorModeBar** ([`anim-editor-mode-bar.tsx`](../../apps/editor/src/components/anim-editor-mode-bar.tsx)) | Chrome `ToolbarStrip` + **State Machine \| Animation Object** `ToggleGroup` `size="sm"`. `data-testid="anim-editor-mode-bar"`. Not a DockView tab. | Animation Graph document shell, above the stacked State Machine and Animation Object DockView surfaces. |
| **IconActionButton** ([`icon-action-button.tsx`](../../apps/editor/src/components/icon-action-button.tsx)) | Icon-only `Button` with `aria-label` plus Tooltip. | Chrome bar, Outliner, Details, Prefab panel. |
| **ContentBrowserAssetTile** ([`content-browser-asset-tile.tsx`](../../apps/editor/src/components/content-browser-asset-tile.tsx)) | Asset card: `--card` thumb well (or image) with a 2px type-colored border on an `absolute inset-0.5` accent box so the bottom stroke stays visible, `--card` text panel, exclusive tap select, paint-select via grid drag, long-press / right-click menu. Pointer events do not bubble to the empty-grid menu. | Content Browser grid. |
| **ContentBrowserFolderTile** ([`content-browser-folder-tile.tsx`](../../apps/editor/src/components/content-browser-folder-tile.tsx)) | Uncolored folder card (`--card` well, muted glyph); tap selects exclusively, double-click navigates. | Content Browser grid (child folders first). |
| **ContentBrowserMoveDialog** ([`content-browser-move-dialog.tsx`](../../apps/editor/src/components/content-browser-move-dialog.tsx)) | Destination picker: item preview, folder search, `TreeView` with muted illegal rows. Move or copy of one or many selected items. | Content Browser **Move…** / **Copy to Folder…** for the current tile selection. |
| **ContentBrowserNewAssetDialog** ([`content-browser-new-asset-dialog.tsx`](../../apps/editor/src/components/content-browser-new-asset-dialog.tsx)) | Large two-pane `Dialog` at CatalogDialog / Project Settings size (`min(90vh, 52rem)` × `min(96vw, 64rem)`): exclusive type cards (Title Case, type-colored thumbs) grouped World / Scripting / 2D / Animation / Rendering / Audio / AI, and a details pane. The Audio group notes that sounds are **Import** (WAV / MP3 / OGG), not created here. Name + type summary stay fixed; for Class, only the searchable Parent Class **tree** (engine + project + enabled-plugin Classes, parent-engine icons) scrolls. Search is not autofocused. | Content Browser toolbar **New Asset**, empty-grid long-press **New Asset**, and empty-grid double-tap. |
| **ContentBrowserSelectionActions** ([`content-browser-selection-actions.tsx`](../../apps/editor/src/components/content-browser-selection-actions.tsx)) | Counted outline **Delete (N)** plus **Deselect All**. Delete is not a filled destructive primary. | Content Browser toolbar when tiles are selected. Confirm is `AlertDialog variant="destructive"`; the list matches **Delete (N)** (folder paths + asset names). |
| **MemberAccessChooser** ([`member-access-chooser.tsx`](../../apps/editor/src/components/member-access-chooser.tsx)) | Small Dialog asking **Get**, optional **Validated Get** (single object/actor instance variables), and optional **Set** after dropping a Class variable onto the graph. Prefab component refs pass `showSet={false}` so only Get / Validated Get appear. `data-testid="member-access-chooser"`; Validated Get uses `member-access-validated-get`. | Class panel `onExternalDrop` for variables, function locals, and Get-only component refs. |
| **SpriteCollisionOverlay** ([`sprite-collision-overlay.tsx`](../../apps/editor/src/components/sprite-collision-overlay.tsx)) | Dashed normalized AABB with 8 resize handles (`Resize Collision East`, …) and interior drag-to-move. Sprite Animation Preview hosts it on the `object-contain` image box so pivot/AABB match non-square textures. | Sprite Preview and Sprite Animation Preview. |
| **NineSlicePreview** ([`nine-slice-preview.tsx`](../../apps/editor/src/components/nine-slice-preview.tsx)) | Read-only still frame of a 2D Panel Texture (or first Material Texture Sample) on a checkerboard, with dashed `--pin-transform` margin lines and orange intersection dots on the `object-contain` image box. Margins stay on PropertyGrid 0–1 sliders (`0.5` = 50% of the source edge; values above 1 stay legacy pixels). `data-testid="panel-nine-slice-preview"`. | Scene Details and Prefab Inspector `2DPanelComponent`. |

Not kit (single call site): `BrandLogo` (docs / wordmark), `BrandIcon` (Homepage rail), `JsBodyEditor` (Inspector Execute JavaScript body).
