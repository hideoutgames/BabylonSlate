# September 2026 exploratory QA disposition register

## Scope and evidence

Source: the supplied exploratory QA brief, compiled 2026-09-07 for the 2026-09-01–2026-09-07 Europe/Oslo window. It reports GitHub Pages testing in editor, Normal Play and Preview Build, streams 50–680, DeepQ / DupPlace131(b) / QA170MARK and fresh QA projects. Historical browser and build revision are unknown. The initial source audit is against `01cc455423b396cf060c1af640f2e1c912655786`.

The original `findings/2026-09-01-andre-bugs/` directory, screenshots, original projects and `CODE-NOTES.md` are unavailable. Searches of the checkout, nearby worktrees, available Git history and remote branch heads found no originals. The supplied brief is the accepted historical source; missing originals do not block a new reproduction. The revised brief establishes stream 690 as **blocked by absent Print HUD output** in Normal Play and Preview despite clean compilation and received Enter input. It is neither a Map Has pass nor a fresh failure; H14 remains historically open on stream 450. This document contains a **new source audit**, not reconstructed original code notes.

Every ID below has been reviewed. Numbered subcases preserve independently testable observations, including overlapping reports. No family is closed by a passing adjacent configuration. Historical severity and confidence are metadata, not newly established impact. `R` means reported reconfirmed; `1` means explicitly one-shot; `U` means no reconfirmation classification supplied. Exact stream anchors refer to the supplied brief, not files inspected here.

Disposition abbreviations:

- **E**: blocked/insufficient current evidence; next action is the stated reproduction. A source candidate is not a confirmed defect.
- **I**: intended behavior under the cited current contract; historical implementation may still have differed.
- **C**: configuration issue under the current contract; preserve supported controls.
- **F**: feature gap or unresolved product decision; no speculative implementation.
- **N**: not reproduced by the stated new test; this does not mean fixed.
- **D**: confirmed defect with a recorded failing test; see implementation results before claiming fixed.

Common accountability and context, inherited by **every subcase** unless overridden: the implementing engineer owns the next technical test; the product owner owns F decisions. Original-fixture recovery belongs to QA if an existing-project case cannot be represented faithfully. Fresh reproductions use isolated copies at the tested revision, with authored/runtime/persisted state, dirty markers and Save/Compile availability recorded independently. No original project is modified. Output-dependent checks use runtime values/counters in addition to Print. Focus-dependent checks distinguish canvas, editor, text field, Play button and modal. Tests use bounded ticks/resources. A passing unit control is not a claim of live-browser or GPU verification.

Priority: W1A state integrity; W1B observation/input ownership; W1C duplication physics; W2 core graph/runtime; W3 remaining rendering/simulation; W4 usability/validation and product decisions. The next-test column also states acceptance: the identified supported behavior must hold, and listed controls must remain intact. Unresolved historical configurations remain E rather than being silently dropped.

## High / Major families

| ID; historical severity/confidence; anchor | Reviewed observations and provisional subcase dispositions | Current expectation / new source evidence | Next test, dependency and priority |
| --- | --- | --- | --- |
| H1; High/Major R; 60/61/80/100/130/131/220/680 | .1 Preview frozen/missing vs Play: E. .2 3D Box Simple Dynamic without Collider tunnels: E. .3 2D Dynamic/Static without Collider tunnels: C. .4 Duplicate×4 explicit Collider gives 1/5 resting vs Place×5 5/5: E, strong identity candidate. .5a Normal Play showcollision only Mannequin: E. .5b Preview no Console: F unless promised. | [Physics](../architecture/physics.md) supports primitive Simple collision in 3D; 2D skips Mesh collision and needs appropriate colliders. `place-actors.ts:duplicateSceneActor` retains component IDs; `instantiate-scene.ts` copies them to runtime GUIDs; `physics-sync.ts` keys colliders by component GUID. Existing physics controls pass, not the duplicate reproduction. | W1C .4: five separated duplicates from identical source IDs versus independently placed controls, queries and settling with Havok and 2D where supported; preserve transforms and prefab references. W3 .1/.2: repeated identical Box/Sphere Simple/Complex/explicit-collider Play/Preview matrix. .3 explicit Box2D on both remains required. .5 count debug shapes separately from console availability. |
| H2; High R; C/50/250 | .1 bare spawned actor: E. .2 ignored transform/origin: E. .3 ~15k after Tick/BeginPlay+Tick: E for invocation/count mismatch, I for spawning every Tick. .4 class value cannot wire to classId: C if actor value rather than class reference. .5 empty classId: I safe null in current runtime; compile feedback F. | `runtime/driver.ts` rejects empty class IDs; `script-host.test.ts` covers class spawn and transform before BeginPlay. Class reference and actor reference are distinct contracts. | W2: valid component-bearing class, one-shot spawn, inspect components and transform before BeginPlay; then bounded Tick count equals actual invocations. Preserve placed-class query. No actor cap/throttle/kill-Z. |
| H3; High/Major R; E/70/340/670 | .1 metallic black silhouette plus orange specular: E, lighting-dependent. .2 Unlit on Box magenta while Mannequin works: E. .3 older invisible DeepQ claim: E, retain refined 340 scope. .4 mid-Play swap: F under no-live-edit v1. | [Materials](../architecture/shader-graph.md); `material-assign.test.ts` passes assignment before/after spawn, rebuild and multipart controls, not actual custom shader GPU compilation. Metallic baseColor alone is not an appearance guarantee. | W3: fixed lighting/IBL/exposure, same material on Box/Mannequin in editor/Play/Preview; capture shader diagnostics and actual rendering. Keep deleted-material fallback H17 separate. |
| H4; High/Major R; 90/100/570 | .1 Delay continuation absent: E. .2 pause/restart cancellation: E additional acceptance check. | [Scripting](../architecture/scripting.md); `script-host.test.ts` verifies tick-clock Delay with pause. Print absence alone does not prove no continuation. | W2 after H5 observation: BeginPlay → Delay(0.5) → state update and Print; direct control; Stop before expiry, restart and verify no old-session continuation. Set Timer is M39. |
| H5; High/Major R; 90/140/210 | .1 overlay-only/no Console/dumplog: I for separate sink contract. .2 BeginPlay Preview overlay absent: E. .3 QA_HOTRELOAD_MARKER → QA: E (also L9). .4 Development Only filtering: I under documented build modes. | Scripting Print is HUD output; debugger Console/log history is separate. DevelopmentOnly remains in Play/Preview and is stripped in release. | W1B: compare emitted full strings, raw runtime state, overlay startup and action-triggered output, modes and filters. Preserve configured sinks; do not add Preview Console or force Development Only OFF. |
| H6; High R family, cross-tab incident 1; 110/50/140/630 | .1 Undo other tab clears Main dirty/Save: E. .2 unsaved edits survive reload: I if autosaved, E for misleading UI. .3 Bake/Export clears dirty: E linked M12. .4 Stop/tab dirty transitions: E linked M3. | `document-context.tsx:stepActiveDocumentHistory` targets active document; `saveProject` snapshots then marks clean, autosave default 120s. [Command layer](../architecture/command-layer.md) defines per-document histories. | W1A: two assets, disk snapshots and independent histories; test clean/dirty/save/Undo/reload transitions, including edit while save is pending. Preserve reported Save All/reload pass; do not call autosave data loss. |
| H7; High R narrowed; 120/520/530 | .1 textured 3D-host tilemap invisible: E. .2 asset tab launches main/open scene: I when matching launch contract. | `snapshot-apply.ts` builds textured tilemaps without a 2D-world gate; `authored-content.test.ts` texture control passes. `play-physics.ts:resolvePlayScene` chooses open/startup scene. | W3: valid texture and camera on fresh 3D host, compare same data on 2D host in both modes. Assert selected launch scene independently of tile rendering. Preserve QA530 control. |
| H8; High R twice same session; 180 | .1 marquee creates Mannequin Copy: E. .2 mode/aria mismatch: E. .3 persistent latch expectation: F; current mode exits on completion. | `viewport-panel.tsx` selection callbacks and `onDragSelectEnd`; toolbar binds pressed state. | W1A: real pointer marquee with placement inactive, actor count unchanged, correct selected IDs and pressed state. Decide one-shot/persistent only if documentation requires it. |
| H9; High U; 190 | .1 right-click extends selection: I. .2 References Close off-screen: E. .3 double-click Open fails: E. | [Engine plan](../engineplan.md) Content Browser explicitly adds right-click target, promises double-click Open, counted Delete. `content-browser-workspace.tsx` references dialog/footer and tile handlers. | W1A: inspect actual Delete targets; long references at supported viewports must leave Close operable; double-click thumbnail/text opens once, context Open control remains. |
| H10; High R twice; 200 | .1 Delete/Undo restores edge without node: E, strong transaction candidate. .2 Redo coherence: E acceptance. | `document-context.tsx:applyGraphChange` applies each `diffGraphCommands` entry separately; RemoveNode precedes RemoveEdge. Per-document content edits must be undoable as coherent actions. | W1A: snapshot valid node/data/edges; Delete, one Undo restores all and compiles, Redo deletes all. Preserve journaling and separate subsequent actions. |
| H11; High U; 210 | .1 intensity runtime-to-editor leak: E historical path unavailable. .2 200→16 clamp: linked M2/M22. .3 transforms unaffected: retained control. | Current `DebugInspectDialog` is snapshot-only with no edit callback; engine plan excludes live Play editing in v1. | W1A: verify current Inspector cannot mutate authoring and runtime component data is detached; test Stop and persisted scene. Do not implement apply-back. |
| H12; High R minimal Ping; 270 | .1 same-graph custom event invocation missing_event: E. .2 tab vs launch compile: E linked M28. .3 save/reload identity: E. | `script-host.test.ts` custom-event control passes; `validate.ts` checks member identity. Creation UI is not invocation proof. | W2: Event Ping + Call Ping, compile both scopes, execute counter, save/reload. Preserve functions and Call Parent Graph; unblock dependent events only on success. |
| H13; High R; 410/420/430 | .1 Fire/F authored but dead: E. .2 G added to Jump dead: E. .3 Space→H ignored/Space active: E. .4 reload restores Space but deletion persists: E separately. Strong load-transport candidate for .1–.3. | [Input](../architecture/input.md) promises authored mappings. Runtime accepts inputMappings, but Play load/player initialization does not forward them. Persistence is a distinct path. | W1B/W2: failing real load test with F/H/G, removed Space and empty bindings; settings save/reload test; fresh/existing and both modes, Stop→Play, focus controlled. Preserve virgin Jump/default controls. |
| H14; High R on 450; 450/460/550/560/690 | .1 class Map Has present false with Get=v: E. .2 pure Make Map same: E. .3 absent/removed key: separate control. .4 690: reconfirmation blocked by absent has/val HUD in both hosts; E. | Map nodes use native membership; existing map tests cover Get/Set/Remove but are not present-key Has closure. Equality/key types matter. | W2 after reliable output: pure and class-writeback present/missing/removed keys, Get/Size/Keys controls; capture evaluated values. First prove an On Action Confirm ? Print alive control; inspect runtime values independently. Blank 690 HUD cannot close Has. |
| H15; High R; 110/120/480/490 | .1 trigger overlap absent: E. .2 solid overlap expectation: C if contract emits Hit only. .3 missing Collider diagnostic/no auto-fix: C plus F for automatic insertion. | Physics contact contract emits overlap for trigger pairs and Hit for solid pairs; collider-bound dispatch, masks and generation flags covered by `collision-events.test.ts`. | W3 after H1/H5: valid layers/colliders/flags, count handlers independently of Print; preserve solid Hit; retain exact valid trigger setup if still failing. |
| H16; High U; 500/510 | .1 direct Jump To State no switch: E. .2 WantsWalk forward rule: E. .3 reverse rule empty: C until authored, then test. | [Animation graph](../architecture/anim-graph.md) targets AnimationGraphComponent; `anim-graph-scripts.test.ts` covers direct jumps, variables, clips and multiple components. | W3 after H13/H21 or direct control: selected state/clip and visible motion, correct component target, complete return rules. Preserve Idle and save/reload. |
| H17; High R; 640/650/670 | .1 placed Class says no inbound references: E, source candidate. .2 assigned Material same: E, source candidate. .3 actor survives class deletion: I if detached snapshot, otherwise policy review. .4 gray fallback: not independent H3 proof. | `assetHeaderDependencies` lacks Scene/Class component reference collection; `project-service.ts` save uses it. Prefab refresh documents live linkage. Deletion contract is reference-aware warning, not automatically cascade. | W1A: actual serialized saved headers/registry inbound index for class/material; warning target/dependency truth; save/reload and closed-tab refs. Preserve sourceId/overrides and prefab refresh. |
| H18; High U; 280/250 | .1 Script Interface Create no asset: E. .2 dockview message cause: E, not established. .3 type switch: L15 independent. | ScriptInterface is supported in engine plan; project service initializes it and successful Content Browser Create closes. | W2: fresh first-click Create, exact type/GUID, open/edit/save/reload. Capture actual errors; do not build another interface system. |
| H19; High intermittent; 270 | .1 Branch prints when Inspector Bool=false: E. .2 Keep Open then next Play loses Tick: E. .3 session Jump no-op: E linked H13. | Runtime state and snapshot display differ; `script-host.test.ts` flow controls pass but not original project lifecycle. | W2 after graph/save/input validity: runtime Bool and invocation counts, fresh and persisted sessions, Keep Open and repeated Stop→Play. Inspector staleness M22 stays separate. |
| H20; High U; 320 | .1 connected-output drag to valid target loses edges: E. .2 empty/invalid release breaks wire: I where documented. .3 partial Undo: E linked transaction investigation. | Scripting wiring policy allows wire break on release; data input replacement differs from exec fan-in/out. `graph-connect` implements policy. | W1A: real drag to valid/invalid/empty targets plus cancel and Undo; preserve unrelated edges and working input→output. Do not prohibit documented wire breaking. |
| H21; High/Med-High R focus-dependent; 250/290/330/370/380/390 | .1 Space places/adds nodes: E. .2 Enter activates menus/closes tab: E. .3 gameplay dispatch while UI owns focus: E. .4 return-to-canvas focus: E strong candidate linked M33. | `input-capture.ts` listens on window; `play-session.ts` skip callback only handles freecam; canvas pointer handler does not focus canvas. Existing focused UI must own keys. | W1B: canvas/text/button/editor/modal matrix, Enter/Space and return click; assert intended owner once, no accidental editor mutation; key release/freecam controls. |
| H22; Med/High U; 230/250/640 | .1 Attach changes parentId without following: E. .2 Outliner child has no runtime Parent ID: C if folder. .3 no editor parenting UI: F. | `script-host.ts` Attach/Detach and `actor-world-transform.ts` support runtime transform parentage; folders are metadata. | W3: parent IDs and world/local following before/after Attach/Detach, compare Inspector; no implicit authored persistence. |
| H23; High/DX narrowed; 140/570/590/600 | .1 no-ground/bad-poly bake error: C setup, E diagnostic adequacy. .2 path empty with bad heights: C unless valid supported input fails. .3 no Path pin: I current From/To API, requested extension F under M43. | Navigation docs; Is Path Valid computes From/To path length. Valid-ground/path controls retained. | W3/W4: good/bad ground and documented height matrix; structured actionable failures, preserve useful diagnostics. No new Path pin or global empty-path claim. |
| H24; Med/High U; 290/300 | .1 CAST FAILED still visible after delete: E retained-history possibility. .2 fresh emissions after actual runtime destruction: E. .3 ended-session emissions: E. | Actor destruction differs from editor deletion of authored copy; historical overlay is not execution evidence. | W2: count new emissions by actor/session before/after runtime destroy and Stop; distinguish retained messages. Preserve valid/invalid cast controls. |
| H25; Med/High U; 280–310 | .1 Make Float 42→0/no dirty: E. .2 Enter/blur/tab/save/compile/reload paths: independently retained tests. | Literal node passes inputs; graph Inspector authors defaults. Concat/Clamp/Add Int persistence is the supplied control. | W2: commit 42, inspect serialized default, dirty/Undo, compile/evaluate/reload; identify commit vs serialization vs display cause before shared refactor. |

## Medium families

| ID; historical confidence/anchor (Medium unless noted) | Reviewed subcases and dispositions | Contract/source and next test / acceptance |
| --- | --- | --- |
| M1; confirmed historical; B | .1 OFF collider wires: I. .2 selected/unselected: retain control. | Physics docs explicitly always draw ColliderComponent editor dashes; Show Collisions controls Mesh dashes. W4: toggle both kinds and selection; do not hide promised editor gizmos. |
| M2; partial; D/340/670 | .1 Spot-only vs Point-only contribution E; .2 Light-on-Mesh E; .3 dual lights on one actor F multiplicity; .4 intensity 200→16 E feedback, F range policy. | `scene-illumination.ts` handles one light per actor; component rows use 0–16 slider while renderer accepts numeric intensity. W3: isolated types and Mesh+Light command/render parity; W4 display/commit/persistence agreement. No max-two scene claim. L29 retained. |
| M3; R; 50/140/630 | .1 Play Stop E; .2 Preview Stop E; .3 tab transitions E. | Play launch saves through `play-context.tsx`; cleaning after successful save may be correct. W1A H6 matrix compares data, not only asterisk. |
| M4; U; 50/100 | .1 Preview wipes history E; .2 multiple completed edits coalesce E candidate. | `stack.ts` merges equal keys; scene property keys remain constant between calls, while command contract says continuous gestures. W1A: separate edits undo separately, a single gesture coherently; preserve histories through Preview. |
| M5; U; 50/170 | .1 ellipsis wrong target E; .2 Shift/Ctrl selection E; .3 multi-edit first actor only E; .4 group Delete Undo E transaction candidate. | Scene diff applies one command per removal; Outliner tests cover menu controls, not full group history. W1A: actual target ID sets and all deleted actors restored in one Undo. |
| M6; Low-Med U; 50 | .1 duplicate same transform I. | `duplicateSceneActor` tests preserve transform. W4: retain same-transform behavior; use controlled separation for H1 collision isolation, no automatic offset. |
| M7; U; 80 | .1 identical Simple+Dynamic boxes differ E. | H1.2 dependency, not automatically duplicate cause. W1C/W3: repeated independent placements with per-actor settling results. |
| M8; U; 90/200 | .1 dirty Class Compile disabled E; .2 no-op Format clears dirty/Compile E. | Scripting compile signature excludes positions intentionally. W1A: compare behavior/default changes with position-only/no-op formatting, stored/compiled versions and save state. |
| M9; U inventory only; 100/540 | .1 no-source Play On Start C; .2 sounds import-only I; .3 attenuation picker rejects E. | Engine plan separates imported Audio from authored Mixer/Channel. W3: valid media import and audible/output playback first, then attenuation assignment; no inventory-as-runtime pass. |
| M10; U; 120 | .1 first tilemap picker choice dropped E. | Asset-reference picker in scene Details should commit selected GUID. W4: first/subsequent picks and save/reload; valid 2D host control. |
| M11; U; 140 | .1 First Index 0 missing_input E; .2 unset and nonzero separate controls. | Validator checks stored defaults, not blanket truthiness. W2: serialized 0→2 loop compiles and increments exactly three times, independent of H5 overlay. |
| M12; U; 140 | .1 Bake clears dirty E; .2 Export clears dirty E; .3 export payload reportedly OK control. | `saveProject`/navigation save hooks may explain clean state. W1A: before/after authored, disk, export bytes and Save availability separately; H6 link. |
| M13; R narrowed; 140/570/590 | .1 raw no-ground Detour diagnostics E; setup failure C. | Navigation contract/valid-ground control. W4: no-ground returns useful setup guidance without suppressing diagnostic detail; H23 link. |
| M14; U; 150 | .1 tiny exec target E. | `graph-nodes.tsx` currently has 44px Handle, 22px visual; touch rules require adequate interactive targets. W4: measure transformed hit area at supported zoom/scales before resizing. |
| M15; U; 160/QA-deep-4 | .1 maximize/restore 8–10s E measurement; .2 selection/gizmo lost E; .3 Play-stop texture retention E. | `scene-viewport-load.ts` defines remount collect/models/warm; resource cache intentionally survives Play. W3: repeated remount and Stop counts/timing; distinguish caching/leak; restore selection/gizmo L6. No invented latency threshold. |
| M16; U corrected; 170 | .1 While does not hang I guard; .2 silent error E presentation. | `infinite-loop.test.ts` controls pass. W1B: bounded graph emits promised diagnostic; never remove execution guard or claim a reproduced hang. |
| M17; U; 170 | .1 false Reload after clean save E; .2 first Keep Open no-op E. | `refreshMtimeSnapshotAfterEditorSave` exists. W1A: clean save/rescan vs actual external modification; controlled first click, correct document preserved. |
| M18; U; 180/200 | .1 Skybox picking E (current renderer marks unpickable); .2a viewport Delete E; .2b Outliner Delete E; .2c graph Delete E; .3 numeric Details clears selection E. | Render picking contract and per-surface focus; W4 actual pointer/key target sets, no selection loss during input. |
| M19; R Near/Far; 180/660 | .1 FOV −90 E validation; .2 Near>Far E validation; .3 camera Undo E. | Camera contract supports valid lens; current renderer guards some ranges, input path needs reproduction. W1A/W4: invalid inputs handled explicitly, FOV 1/179 stay valid, one completed component edit reversible. |
| M20; U; 190 | .1 New Folder stray Rename E; .2 Duplicate stale grid E; .3 Move stays open E; .4 no tree context I; .5 folder double-click E; .6 plain click retains multiselect E. | Engine plan Content Browser says no tree context, click replaces, double-click enters. W4 separate successful/failed action and refresh tests; do not blanket-change modal policy. |
| M21; U; 200 | .1 graph Ctrl+C/V E support/dispatch; toolbar Paste control. | Graph clipboard toolbar exists; shortcut expectation needs current UI contract. W4 graph-focused shortcuts with text-field editing preserved. |
| M22; U; 210 | .1 Inspector not live E snapshot policy; .2 clamp feedback E; .3 modal hit geometry E; .4 Compiler Results empty E scope. | Debug Inspector snapshot; graph diagnostic scope intentionally suppresses hidden-document publishing. W1B: same graph/revision/scope, actual values and click positions; H5/H11/H19 links remain separate. |
| M23; U; 220 | .1 desktop joystick default I; .2 picking interception E; .3 absent Play joystick E configuration/scope. | Engine plan explicitly defaults editor joystick true, persists false. W4 outside-bounds picking and OFF control; runtime toggle evaluated separately. |
| M24; U; 230 | .1 Locked permits edits E scope; .2 no local/world mode F; .3 two rotated Scale handles E visibility/occlusion. | Viewport excludes locked actors from gizmo roots. W4 test gizmo, numeric editing, selection separately; establish lock contract; no automatic new gizmo system. |
| M25; U; 270 | .1 Preview launches with compile errors E; Normal Play modal control. | `play-preview-prepare.ts` and compile scope contract. W1B: invalid referenced graph versus unrelated invalid graph, actual build payload and error ownership; no assumed identical UX. |
| M26; U; 270 | .1 Console caret resets E. | `debug-console.tsx` controlled stable Input, no explicit per-key reset found. W1B actual typing/insertion/deletion/selection/paste and history/completion controls. |
| M27; U; 270 | .1 Class vars absent editor Details F unless promised exposed; .2 Float Fn missing runtime E. | Class/local/function identities and exposure metadata differ. W4 test promised exposed values and runtime snapshot separately. |
| M28; U; 300 | .1 tab compile passes vs launch missing_event E; different scopes I where documented. | `graph-validation.ts` dependency/saved-version scope. W2 H12/M8: same class identity and versions, referenced and unrelated graph controls, no stale status. |
| M29; U fresh too; 310 | .1 second wire rejected only at compile E actual path; .2 Add Node same coords E; .3 Format no-op E. | Documented data replacement vs exec fanout; graph-connect control. W1A/W4: cardinality and edge identity independently from placement/format; L2 retained. |
| M30; U corrected; 380/390 | .1 unwired impure Append nonexecution I; .2 missing feedback F. | Impure execution requires reachable Exec; pure immutable Append needs accumulator writeback. W4 preserve pure length4 and class Acc1…5. |
| M31; U; 400 | .1 On Action phase missing_input E contract mismatch candidate; .2 click wiring E; .3 drag control retained. | Catalog phase required STRING while compile defaults non-released to pressed. W2 establish required/default direction then test diagnostics and actual dispatch separately. |
| M32; U; 410 | .1 scalar Get Axis Move I; .2 absent On Axis F. | Input docs define scalar magnitude; GetAxis2D exists. W4 preserve 0/1 sampling, document appropriate node rather than new API. |
| M33; U; 420 | .1 Space activates focused Play button I; .2 focus remains after canvas return E. | Native button semantics vs input-capture canvas focus omission. W1B H21: button→canvas→Space triggers gameplay once without reactivating button; preserve keyboard UI access. |
| M34; U; 610 | .1 no texture skipped warning I/C; .2 reversed life E UI, normalizer control passes; .3 Pre Warm9999 E UI, cap control passes; .4 Capacity0 plus Emit Rate999999 silently becomes16: E feedback, minimum I; canonical report for duplicate L27. | Particle docs require texture, capacity16–4096 and capped prewarm. `particle-payload.test.ts` verifies reversed lifetime normalization and prewarm≤60. W4 safe input/blur/save/reload normalized display tests; no extreme runtime. |
| M35; unavailable; 610 | .1 missing UserInterface/game HUD I removed scope. | Engine plan explicitly removes system. W4 only test misleading current advertised option; do not re-add system. |
| M36; U; 660 | .1 duplicate camera names I if stable IDs; .2 ambiguous picker E usability. | Scene Details stores actor+component IDs. W4 choose each same-name camera, verify distinct persisted target; labeling changes only if needed. |
| M37; U; 640 | .1 Unicode/emoji glyph replacement E font coverage/configuration. | Font loader and actual bundled face coverage needed; 2D bitmap behavior does not prove 3D issue. W3 supported/missing glyph controls and explicit fallback scope. |
| M38; partial; 580/620 | .1 BT execution E current reproduction; .2 per-node diagnostics F; .3 Run BT F; .4 AIController F. | BT component/evaluator supported, MoveTo reported pass. W3 runtime state assertions then separate tooling decisions; no implied subsystem creation. |
| M39; U; 260 | .1 broken Delay H4 E; .2 absent Set Timer F. | Existing Delay contract does not promise new timer API. W2 Delay acceptance; W4 product owner decides timer backlog. |
| M40; Med/High R; 260 | .1 sustained freefall E collision cause; .2 no kill-Z F. | No established world-boundary requirement. W4 bounded counts/positions only; H1 fix must stand without kill-Z. |
| M41; R surface-dependent; 70/80/640 | .1 Visible=false Box/Ground E; .2 Outliner hide E surface; .3 Light hide E enabled-vs-visible policy. | Scene instantiation copies visible; driver snapshot flag and `setPlayVisualVisibility` handle parts. Light has separate enabled contract. W3 test actual authored visibility across modes; preserve hidden 3D Text, keep L31 separate. |
| M42; U; 320 | .1 inline literals display-only I. | Scripting docs explicitly use read-only canvas previews and Inspector editing. W4 verify advertised Inspector edit works; inline editing feature is deferred. |
| M43; Medium soft/API R; 590/600 | .1 cannot wire Find Path To.Path into Is Path Valid: F API extension; current From/To contract I. | Current navigation API computes validity from From/To rather than accepting an existing path. Product owner decides whether a path-array overload is warranted; preserve working valid-ground/good-height From/To controls. L26.1 is the duplicate; height sensitivity remains H23/L26.2. |

## Low / Info / Soft families

| ID; historical label/confidence/anchor | Reviewed subcases and dispositions | Contract/source and next test / acceptance |
| --- | --- | --- |
| L1; Low U; 90 | .1 illegal cycle dropped I; .2 feedback E/F. | Scripting forbids exec/pure cycles but permits cached impure data feedback. W4 test appropriate graph/edge type, preserve guards. |
| L2; Low R; 90/250/310 | .1 same coordinates E; .2 accidental double insert E. | Separate position policy from actual action count, M29. W4 one activation→one node, then multiple insertion placement. |
| L3; Low U; 120 | .1 Add Component remains E policy; .2 New Asset remains E; .3 Pick remains E policy. | Successful CB creation explicitly closes. W4 test each independently; no blanket close-on-add redesign. |
| L4; Low U; 140/360 | .1 Development Only default ON I; .2 hint absent F. | H5 build filter contract; W1B verify configured output and release stripping, no default change. |
| L5; Low U; ~150 | .1 search spaces E; .2 no accessibility refs E insufficient automation evidence. | Node palette has option roles/keyboard handlers. W4 actual multiword input/results, named dialog, focus and keyboard use. |
| L6; Low U; 160-C | .1 layout loses selection/gizmo E. | M15.2 explicit retained case; W3 remount with selected actor, restore actual selected IDs and visible gizmo. |
| L7; Low U; 190 | .1 missing asset DND F outside promised tree gesture; .2 Escape dismissal E; .3 Sort remains E policy. | Engine plan promises tree hold-to-drag reparent. W4 test promised path, context Escape and Sort independently. |
| L8; Low R; 200 | .1 wrong pin type rejected I; .2 feedback absent F unless promised. | Type safety retained; W4 actual interaction and existing feedback contract. |
| L9; Low U; 210 | .1 QA_HOTRELOAD_MARKER→QA E linked H5.3. | W1B compare full raw command, DOM text and clipping; preserve exact string reproduction. |
| L10; Soft U; 220 | .1 2D launch uses 3D camera/skybox C if configured 3D scene, otherwise E. | Launch contract H7.2; W3 same intended scene/camera with QA530-style fresh 2D control. |
| L11; Low U; 270 | .1 event rename absent F; .2 delete absent F unless advertised. | Separate member-management support from H12 invocation. W4 test supported affordances; product decision for new ones. |
| L12; Low U; 280 | .1 Add Function modal remnants E. | W4 actual nested-layer visibility/interactivity after completion/cancel; don't mistake intentional parent dialog for stale overlay. |
| L13; Low U; 370 | .1 untouched Make Rotator missing_input E; .2 untouched Vector3 E; .3 committed defaults control. | Validator stored-default semantics; W2 compile/evaluate untouched valid defaults and compare persisted edits, H25/M11 relation unproven. |
| L14; Soft U; 370 | .1 Vector3-only Look At I; .2 actor overload F. | Node API accepts From/Target vectors; W4 preserve Find Look At, no new overload without requirement. |
| L15; Low U; 250 | .1 first Create flips Script Interface E. | New-asset dialog has selected-type test and type=button cards/Create. W2 real first click after exact intended selection, correct asset output; H18 relationship unproven. |
| L16; Low U; 250 | .1 Add Node text overlap E. | W4 actual node palette supported viewport/zoom/font and keyboard targets. |
| L17; Low U; 250 | .1 quotes required for spaces I; .2 unquoted extra token resolves wrong actor E diagnostic. | Debugger parser supports quoting. W4 compare quoted exact name, invalid extra-token usage and unambiguous ID. |
| L18; Low U; 260 | .1 TypeError reading document E missing stack/action. | W1B collect fresh stack, action, browser, revision and effect before any root-cause patch. |
| L19; Info U; 260 | .1 missing dockview ContextMenu notice E impact. | W4 reproduce a supported failed action, distinguish optional-module notice; no blanket dependency change. |
| L20; Low U; ~100 | .1 dual Mesh labels F usability unless selection ambiguous. | Multiple meshes render control; W4 select/edit each distinct component while preserving both. |
| L21; Low U; rollup | .1 actorboundingbox Boolean argument I. | `debugger/commands.ts` Boolean toggle and parser/registry tests. W4 verify help/syntax and toggle, not actor-ID redesign. |
| L22; Info explicit nonbug; 250 | .1 Tags absent F accepted scope. | No implementation without separate requirement; product owner backlog only. |
| L23; Info U; 230/500 | .1 Timeline/Sequencer absent F; .2 Play Animation/Montage nodes absent F. | Check advertised inventory; latent-node prose does not authorize whole systems. W4 product owner scope, preserve AnimationGraph runtime. |
| L24; Info U; 640 | .1 networking/replication No Types I excluded scope. | Engine plan no multiplayer/networking. W4 misleading current discoverability only, no networking implementation. |
| L25; Soft U; 640 | .1 empty 3D Text white square F semantics unresolved. | W4 compare empty/whitespace/nonempty and actual font; product owner decides placeholder/validation policy. |
| L26; Soft U; 600 | .1 no Path pin: duplicate of M43 (Medium API gap); .2 height sensitivity E/C matrix. | Is Path Valid uses From/To, H23. W3 valid-ground/agent-height controls; no new pin. |
| L27; Low/Med U; 610 | .1 Capacity0→16 minimum I; .2 silent/stale feedback E. | Particle docs capacity16–4096; M34.4 next input/commit/display/reload test. |
| L28; Minor U; 70 | .1 DestroyActor(null) compiles/no-ops I; .2 feedback F. | Runtime safe null guard; preserve DestroyActor(Get Self). W4 no new failure contract without requirement. |
| L29; Minor U; 70/670 | .1 second LightComponent no contribution F multiplicity decision. | M2.3 retains exact actor arrangement; one-light-per-actor implementation isn't proof of intended multiplicity. W3 product contract then test, no silent generalization to scene light count. |
| L30; Low U; 50 | .1 synthetic Place Actors click miss E driver evidence only. | W4 manual pointer/keyboard activation and actor creation result; synthetic miss alone never confirmed defect. |
| L31; Low U; 50 | .1 Directional Light hide ignored Preview E visibility/enabled policy. | M41.3 separate Preview case; W3 test helper visibility and actual light enabled independently. |
| L32; Low U; 270 | .1 first Console click E; .2 first Inspector click E; .3 toast hit interception E. | Play chrome/debug menu inputs. W1B real pointer with/without overlapping toast and return-to-canvas; preserve interactive toast actions. |

## Leftover tracks and unblock conditions

| Track | Owner / next action |
| --- | --- |
| Map Has and stream 690 | Implementer: prove Confirm ? Print alive, then new pure/class present-key tests with runtime value controls. The supplied 690 reconfirmation is HUD-blocked, not a pass/fail flip; preserve the standing 450 failure. |
| Earlier Map Remove/Size/Keys driver blockage | Covered historically by 550/560 writeback controls; retain regressions, not another unresolved global failure. |
| WAV/MP3 import and Play Sound | Implementer: valid provided test media, native import/source/invocation/output. Human assistance only if required native action unavailable; report unrun audio honestly. |
| Channel/Mixer/attenuation playback | Implementer after basic source playback; inventory540 is not runtime evidence. |
| Spot-only vs Point-only | Implementer: controlled single-light scene M2/H3. |
| Mid-Play material swap | Product owner: current v1 no-live-edit boundary; test an existing supported path if identified. |
| Generate Hit/Overlap Events UI | Implementer checks actual actor flags/catalog; product owner decides new affordances. Is Trigger existence is not generation-flag proof. |
| Animation forward/reverse transitions | Implementer after direct reliable state control or H13/H21; fully authored return rule, component/state/clip evidence. |
| 3D-host tilemap | Implementer: valid texture, launched scene and camera, compare 2D control. |
| BT diagnostics / Run BT | Implementer verifies existing component execution; product owner decides additional tooling. Preserve MoveTo walk. |
| Is Path Valid / heights | Implementer: current From/To API, valid-ground/agent-height matrix and diagnostic scope. |
| Widget/HUD and networking | Product owner: retain current removed/excluded scope; only misleading advertised features may warrant fixes. |
| Script Interfaces | Implementer: H18 create/open contract before promised dependent usage; dependent failure not inferred. |
| Custom-event calls | Implementer: minimal Ping H12 then follow-on calls; preserve functions workaround as control. |
| Input load and persistence | Implementer: add/rebind/delete/reload, Stop→Play, fresh and representative existing settings, both hosts; no Fire-only closure. |
| Duplicate collider1/5 | Implementer: exact logical duplicate-vs-placement configuration, explicit and supported implicit shapes; stream680 never closes duplication. |
| Runtime camera destruction / active-camera switching | Not exposed in stream660: read-only Inspector and no console commands. Product owner determines supported control surface; implementer tests any existing runtime API independently. No claim of original UI reproduction. |
| Missing CODE-NOTES | Unavailable original; this audit is newly collected work. No fabricated file reconstruction. |

## Controls and corrected hypotheses

Retain the supplied passing configurations: Sequence/Branch, Do Once, Do N exactly N, ForEach, Compare, Select, Switch, Flip Flop, Gate and bounded While; integer literals/Add Int2+3, Array Length/Concat/Contains/Get/RemoveAt and safe out-of-range; pure Append length4 and class Acc1…5; Map Get/Set, Size/Remove with Set MACC and Keys[a]; placed-class queries, offsets, self-destruction, valid/invalid casts, functions, custom-event creation and Call Parent compilation; location/rotation/forward/FindLookAt; virgin Jump and Move0/1; collision masks, explicit Box/Sphere colliders and solid Hit; dual Mesh rendering, fresh2D tilemaps, Idle clip and hidden3DText Preview; Save All/reload, AnimGraph/settings persistence and both prefab-refresh controls; valid-ground bake/path and BT MoveTo; Pause/Step/Esc, FOV1/179 and working Stats/Console/Joystick toggles. Audio inventory is inventory only; showcollision/Preview Console limitations were documented, not fixed.

Do not reopen as stated: scene-wide two-light maximum; fully invisible metallic meshes; reproduced While hang; globally broken Jump; pure Append automatically mutating its input; always-empty Find Path; globally broken Map Size/Remove. A new contrary finding needs a new scoped configuration and must preserve the old passing control.

## Gate and implementation results

Gate A source/contract review: all 25 H, 43 M and 32 L families (100 numbered IDs after the revised brief) and listed variants considered before production edits. Remaining E/F rows have explicit owners and next actions above. Each proposed fix still needs its own failing reproduction/test before behavior changes. No report is fixed merely because it was reviewed.

Baseline controls on the audited revision:

- Physics sync, software/Havok controls, pairing, particles, authored render content, material assignment and collider visuals: **8 files / 93 tests passed**.
- Script host, collision events, animation graph scripts, loop guard, Play load and Map controls: **6 files / 81 tests passed**.
- These runs are lower-level tests. They do not establish original-project, live Pages, native audio, GPU appearance or full focus-matrix outcomes.

Implementation entries will record failing and passing tests, exact subcases claimed, changed files and residual checks. Gate B requires current-revision evidence and relevant passing controls, accurate persistence/reference behavior, full local verification and repository delivery gates. E, F and N must never be relabeled fixed to finish the register.

### H10.1–.2 and M5.4: coherent deletion history

- New desktop Chromium reproductions failed before the change: one Undo restored an edge without its deleted node (H10), and failed to restore the complete deleted actor subtree (M5.4).
- `EditSession.applyBatch` now keeps the graph/scene diff from one edit in one history entry. Graph removals record original positions; removals run in descending order so inverse insertion restores ordering. Individual journal deltas remain serializable, including optional graph positions; old entries without positions retain append behavior.
- After the change, both browser reproductions pass Delete → Undo → Redo, with graph compilation, repeated Undo and an untouched actor control. All 89 edit-package tests pass, including structural equality, separate document history, byte-budget eviction and empty-batch redo preservation.
- These subcases are confirmed defects with a locally passing fix, pending full verification. M5.1–.3, H20 drag variants, separate-edit coalescing (M4), crash recovery after Undo and historical project-specific variants remain unclosed.

### H17.1–.2: saved and open-document class/Mesh references

- A new storage-backed test reproduced empty Scene dependencies after assigning a real user Class and Material. Saves now collect typed actor/component class references and Mesh material/model guids; Class identity resolves from the compiled path-based ID. Navmesh/audio-reverb saves retain the dependencies and reindex the header.
- Saved replacement/reload/bake and negative identity/text controls pass: **114 tests across asset-service and Content Browser helpers**. The stored payload stays intact and replacing Material A with B removes the saved Scene inbound edge to A.
- A desktop browser test reproduced the missing placed-Class warning for an open Scene. Delete and Show References now include typed references from already-loaded documents. Both browser tests pass, covering the placed Class and an unsaved Material assignment; Cancel preserves the asset. The warning names referrers without promising actor deletion or cascade behavior.
- These tested variants are confirmed defects with a locally passing fix, pending full verification. Older unopened headers remain stale until resaved. Other component/reference kinds and original unavailable projects remain unclosed; this is not a universal reference-collector claim.

### H13.1–.3 and H21/M33: authored input and keyboard ownership

- Load tests establish that authored F/H/G bindings were absent from runtime initialization. Normal Play now forwards project mappings through its load message, and exported manifests forward them to the player. Explicit empty lists remain empty during project/runtime normalization; absent legacy mappings still use defaults.
- Focus regressions cover canvas startup, text/button ownership, return by pointer, held-key release on blur and removal of old-session listeners in both capture hosts. A separate failing resolver test exposed complete between-tick taps being lost; action transitions now survive those batches.
- Three desktop Chromium checks pass: author F, add G, replace Space with H, Save All/reload and dispatch in both Normal Play and Preview Build; native toolbar Space activation followed by canvas ownership. The runtime-ready wait is part of the fixture. Unit load/resolver/capture tests also pass.
- These tested paths have locally passing fixes, pending full verification. The original H13.4 report of a particular replacement reverting to Space was not reproduced by the fresh browser test. The empty-list correction is separately demonstrated by serialization/resolver tests. Original projects, touch/modal/editor focus variants and every historical session configuration remain unclosed.

### H1.4: duplicate component identity

- A controlled five-box test reproduced tunneling after Duplicate with the software backend in both 2D and 3D; independently created boxes passed. The same explicit-collider settling configuration passed with Havok and Rapier before the fix, so this is not evidence that every backend reproduces the original 1/5 outcome.
- Duplicate now assigns new component IDs and remaps their parent hierarchy while retaining prefab source identity, overrides and transforms. All eight backend/control combinations pass settling, per-box traces and survival after destroying the source actor. The placement/hierarchy suite also passes (41 tests total across two files).
- This is a confirmed duplicate-identity defect with a locally passing fix, pending full verification. Existing saved duplicates are not migrated. Historical Pages backend selection, exact original project, implicit Simple/Complex variants, browser rendering and H1's other subcases remain unclosed.

### H4/H5/H14 controls and an independently discovered generic-default defect

- Seven compiled-runtime tests cover pure Map and class writeback with present/missing/removed keys, Get/Size/Keys, exact Print marker text, and Delay state changes with pause/resume and Stop cancellation. Map Has itself was not reproduced as failing in these configurations.
- Two desktop browser checks pass in Normal Play and Preview Build: BeginPlay prints the complete `QA_HOTRELOAD_MARKER`, Delay prints its continuation, and an On Action Confirm control prints `true` and `v` from a pure Make Map. These are fresh checks, not a replacement outcome for historically blocked stream 690.
- The negative-key controls separately found that compilation used unresolved generic pin types for defaults: a missing String Map value became `null` instead of `""`. Four runtime tests failed on this value. Compilation now uses the resolved type view without rewriting stored pins. All 434 tests across the targeted compiler, node and runtime suites pass.
- H14's historical claim, existing-project variants and key-identity cases remain open. H4/H5's original projects, configured output/build modes and other message histories remain unclosed; the current fresh controls are N, not claims that every historical report was fixed.

### H6: edits arriving during Save All

- A failing document-service regression established that Save All cleared dirty state for edits made after its captured revision, including a different document. Saves now capture document snapshots and compare content identity at completion. A stale older completion conservatively marks a newer in-memory revision dirty again.
- A separate storage test reproduced concurrent journal appends losing the first edit. Appends/clears now serialize per project/storage; a stale save cannot clear recovery. The queue permits subsequent edits after a storage error. Four journal tests and 29 document-service tests pass.
- A desktop browser fixture held the actual OPFS Class file write open, edited through the command layer, and released the write. Save All stayed enabled, the journal survived, disk held the first revision, and a second Save All/reload retained the later edit. Editor typechecking passes.
- These are confirmed save/dirty and journal-concurrency defects with locally passing fixes, pending full verification. The original cross-tab incident, Bake/Export/Stop variants, recovery after Undo and unavailable historical projects remain unclosed.

### H25 and M4: literal default keys and history boundaries

- A new Inspector regression reproduced `0` after writing `42`: the row wrote `default:In`, while reading/codegen preferred an existing `default:in`. Property rows now write canonical pin IDs; variable-default and nested-Structure adapters follow the same key contract. All 38 Inspector helper tests pass.
- Separate stack regressions reproduced merged-edit Undo → Redo → Undo restoring an intermediate value and a new edit after Undo merging with older history. Redo now retains the original inverse. Explicit UI gesture boundaries separate completed typing/pointer edits while retaining continuous typing and multitouch grouping; 93 edit/gesture tests pass.
- Three desktop browser tests pass: Float 0 → 42 → 43, Undo to 42, Save/reload/Play printing 42; graph Delete/Undo/Redo; actor-subtree Delete/Undo. The test fixture uses the actual literal node's pin declarations, since raw test-hook nodes without metadata do not expose Inspector defaults.
- These tested variants have locally passing fixes, pending full verification. H25's unavailable original project and all other commit/tab paths, M4 Preview lifecycle and other editing surfaces remain unclosed. The default-key and history defects are distinct causes, not a single explanation for all observations.

### H6/M4/H10 continuation: recovery after history and asset edits

- A fresh browser regression reproduced an undone node move returning after recovery. Undo/Redo now append their applied commands and schedule autosave through the same path as ordinary edits. Multi-command actions serialize as one replayable batch; an unknown child skips the entire batch. Legacy individual command lines remain supported.
- A second reload after recovery reproduced a missing journal. Recovery now retains unsaved commands until Save or clean Close. A separate Enum fixture reproduced a lost member because recovery ignored non-graph/scene documents; target opening and replay now cover authored asset document kinds and leave untouched documents clean.
- Both browser regressions and the batch replay/session unit tests pass; typechecking passes. These results close the tested recovery gaps. They do not close the remaining historical cross-tab, Bake/Export/Stop, or unavailable-project variants.
